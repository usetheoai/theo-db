# Benchmarks — archive

Artefatos de benchmark de milestones já lançados que **não são citados** por nenhuma
superfície viva (README/CLAUDE/ROADMAP/CHANGELOG/PRD), ADR, nem pelo audit trail
(`knowledge-base/`). Movidos para cá (não apagados) para desafogar o topo de
`docs/benchmarks/` mantendo a evidência reproduzível — conforme
`.claude/rules/audit-trail-rotation.md` (**arquivar, nunca deletar** evidência).

- **Nada foi perdido:** cada arquivo continua buscável e versionado.
- **Zero links quebrados:** só entraram aqui os `.md` com 0 referência em todo o repo.
- **Regra viva:** benchmark é evidência (`public-copy.md §4`, measurement-first). Se um
  destes voltar a ser citado por uma claim, mova-o de volta ao topo de `docs/benchmarks/`.
