# Review backlog — develop (diff)

- Iteration: 0
- Verdict: IN_PROGRESS
- Files fully reviewed: 14 / 14
- Open defects: 37 (4 blocking, 33 advisory + 0 systemic group(s))
- Advisory acknowledgements (observation/positive): 18

## Pillar status (derived)

| Pillar | Status | Files done | Open findings | Blockers |
|---|---|---|---|---|
| code | COMPLETED | 14/14 | 4 | 0 |
| architecture | COMPLETED | 14/14 | 4 | 0 |
| system_design | COMPLETED | 14/14 | 5 | 0 |
| design_patterns | COMPLETED | 14/14 | 2 | 0 |
| semantic_folders | COMPLETED | 14/14 | 3 | 0 |
| security | COMPLETED | 14/14 | 0 | 0 |
| tests_deadcode_docs | COMPLETED | 14/14 | 4 | 0 |
| license_provenance | COMPLETED | 14/14 | 27 | 0 |
| idiomaticity | COMPLETED | 14/14 | 2 | 0 |
| maintainability_extensibility | COMPLETED | 14/14 | 4 | 0 |

## Open blocking findings (must fix)

- [HIGH] code theodb_rs/src/am/columnar_project.rs:203 — subxact_clear clears only the ACTIVE slot (swap_active(None)) on SUBXACT_EVENT_ABORT_SUB but never prunes NODE_PROJECTIONS; the registry is cleared only by clear_all() at OUTER xact end. After a caught PL/pgSQL EXCEPTION that longjmps out of a projection scan before end_custom_scan runs, its {node_ptr -> wanted} entry is orphaned for the rest of the transaction. A later ProjScanState palloc'd at the same address (ABA) whose begin takes the columns_needed==None fallback (whole-row/system-column, e.g. SELECT ctid FROM t_col) does NOT registry_insert, so exec_custom_scan's registry_get(node) at line 459 reads the STALE wanted set and installs it as the active projection -> columnar decodes only the stale columns and NULLs the rest -> silent wrong answer. The module's own clear_all note (lines 105-107) says clearing 'closes the ABA window where a new scandesc ... could inherit a stale projection', but that guarantee holds only at xact end, not across a subxact abort.  (finding 467be8f2a7ad7f1b)
- [HIGH] code theodb_rs/src/am/columnar_project.rs:407 — begin_custom_scan insere no NODE_PROJECTIONS so no ramo Some; o ramo None (fallback) nao insere nem remove. subxact_clear limpa so o ACTIVE slot, nao o registry (clear_all so em top-xact). Trigger: Q1 (Some) sofre EXCEPTION num subxact PL/pgSQL -> end_custom_scan pulado -> entrada {addr->wanted} orfa; Q2 (None, ex SELECT ctid) reusando o addr NAO remove -> exec le a projecao stale -> decodifica so colunas stale -> resultado NULL errado silencioso. 3 analises convergiram (council rust-pgrx, council index-storage, pilar code). O irmao customscan.rs e imune (insert incondicional + flag membership_active). Remediacao unanime: None => registry_remove(node).  (finding 21c3ac0b9ed948b7)
- [HIGH] tests_deadcode_docs theodb_rs/src/am/columnar_project.rs:69 — Nested-scan projection correctness — the SINGLE reason the side channel is keyed by the scan descriptor — has zero regression test. No test drives a subplan that scans a DIFFERENT theodb_columnar table during qual evaluation while an outer projection scan is active, i.e. the exact scenario the scandesc key exists to prevent from silently projecting the wrong columns.  (finding defbb19112435cd0)
- [HIGH] tests_deadcode_docs theodb_rs/src/am/columnar_project.rs:578 — The `columns_needed` decode-all fallback (the `att <= 0` None branch: whole-row Var + system column) — described in-code as the ADR-2 'correctness floor' — is NOT exercised by any test. `test_fallback_whole_row` runs `SELECT a, b, c` (all attno >= 1), which registers wanted={0,1,2} and never returns None; the plan's promised `SELECT ctid` / whole-row A/B test was silently dropped.  (finding 207c4decea7e9553)

## Advisory defects (not blocking)

- [MEDIUM] architecture theodb_rs/src/am/columnar.rs:698 — M149 added a SECOND column-projection stripe-decode seam (`decode_stripe`/`form_row` threading `want_mask: &[bool]`) instead of consuming the pre-existing `decode_columns(projection: Option<&[usize]>)` seam that the plan's ADR-3 prescribed. The 'skip read_chunked+zstd on unprojected columns' knowledge now lives in two paths with two encodings, exactly the alternative ADR-3 rejected as DRY-breaking.
- [MEDIUM] architecture theodb_rs/src/am/columnar_project.rs:51 — The CustomScan side-channel correctness protocol — thread_local ACTIVE slot + swap_active + RAII ActiveGuard(Drop) + node-keyed registry (insert/get/remove) + clear_all + Xact/SubXact abort-clear callbacks — is duplicated near-verbatim from customscan.rs (the vecfilter). Two independently-maintained copies of a subtle install/restore-on-longjmp/clear-on-abort protocol will drift.
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:44 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:72 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:98 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:126 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:153 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:179 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:206 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:232 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:258 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:289 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:316 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:343 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/references-catalog.md:370 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance CHANGELOG.md:1 — commit history signals third-party origin: 1bf3df0 “vendor(rabitq): core RaBitQ (Apache-2.0) p/ futuro IVF-RaBitQ (Gap 2, ADR-0032)” touched this file
- [MEDIUM] license_provenance CHANGELOG.md:1 — commit history signals third-party origin: 8ec48dd “chore(release): v0.63.0 — HNSW extendCandidates (recall 0.974->0.990) + RaBitQ vendor + veredito P0” touched this file
- [MEDIUM] license_provenance CHANGELOG.md:478 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance CHANGELOG.md:495 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance CHANGELOG.md:510 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance CHANGELOG.md:816 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance CHANGELOG.md:1292 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance ROADMAP.md:1534 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance docs/benchmarks/m149-artifacts/m149-clickbench-ab.json:573 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] maintainability_extensibility theodb_rs/src/am/columnar_project.rs:212 — pathlist_hook has measured cyclomatic complexity 20 (McCabe), above the lizard default threshold of 15 — the flagship M149 planner hook concentrates AM-detection guards, aggregate-query exclusion, and the cheapest-seqscan search in one body.
- [MEDIUM] system_design theodb_rs/src/am/columnar_project.rs:271 — Scalability — missing_required: the projection CustomPath is serial-only, so at ClickBench/columnar scale the planner picks a parallel seqscan and projection pushdown silently does not engage (fail-safe but the 3.73x win vanishes exactly in the large-analytic regime the columnar store targets).
- [MEDIUM] tests_deadcode_docs docs/benchmarks/m149-projection-pushdown.md:54 — Doc<->code drift: the benchmark doc states `SELECT *` (whole-row) makes columns_needed return None -> decode-all. Postgres expands `SELECT *` into per-column Vars (attno >= 1), so columns_needed returns Some(all-columns), never None. The A/B outcome (byte-identical) is correct, but the stated MECHANISM is wrong, and it masks the fact that the true None fallback (finding #1) is untested.
- [MEDIUM] tests_deadcode_docs theodb_rs/src/am/columnar_project.rs:479 — Three functional/defensive paths carry no test: rescan_custom_scan (nested-loop inner re-execution, line 479), the `theodb.enable_projection = off` fallback to the plain seqscan (line 221), and the subxact-abort active-slot clear after a caught PL/pgSQL EXCEPTION (line 199-204). The first is a real result-bearing path (a nested-loop join re-scanning the columnar inner) that would produce wrong rows if broken.
- [LOW] architecture theodb_rs/src/am/columnar_project.rs:114 — `struct Methods<T>(T); unsafe impl<T> Sync for Methods<T> {}` — the blanket-Sync wrapper for CustomScan method tables — is copy-pasted into a third CustomScan module. A hand-rolled `unsafe impl Sync` replicated three times is a soundness surface that should be declared once.
- [LOW] code theodb_rs/src/am/columnar_project.rs:268 — The projection CustomPath is priced at 0.9x the seqscan total_cost UNCONDITIONALLY (`let total = (*seq).total_cost * 0.9;`), independent of how many columns are actually projected. For a whole-row / SELECT * query columns_needed returns Some(all-columns) so the node decodes every column (want_mask all-true == pre-M149 work) yet still wins the plan by 10% while adding the side-channel install + registry churn per pull — a net-negative plan for the zero-benefit case. Contract smell: the cost claims a saving the node does not deliver when nothing is projected away.
- [LOW] code theodb_rs/src/am/columnar_project.rs:268 — O path e priced 0.9x o seq com disabled_nodes=0 (alloc_node zera). Sob enable_seqscan=off (PG17+ marca disabled_nodes=1), o nosso path fica 0 e domina o seq que o usuario desabilitou. Resultado correto, so surpresa de planner-preference.
- [LOW] idiomaticity theodb_rs/src/am/columnar_project.rs:63 — swap_active hand-rolls RefCell::replace via mem::replace(&mut *borrow_mut(), v); the std method exists.
- [LOW] system_design theodb_rs/src/am/columnar_project.rs:459 — Observability — missing_required: the projection path degrades silently to decode-all in two runtime cases (columns_needed==None fallback, and non-projection scans) with no counter/log to see it; a plan change that stops engaging the node is invisible except by manually re-running EXPLAIN.

## Observations & positives (advisory — not defects)

- (observation) architecture theodb_rs/src/am/columnar.rs:1067 — Coupling-direction note (not a fixable defect): the low-level storage TableAM `columnar.rs` compile-time depends UP on the planner-integration module `columnar_project::scan_projection`, and the cross-module contract is an untyped `usize` raw-address key. This is essential (the PG18 TableAM `scan_begin` carries no column bitmap — ADR-1) and mirrors the accepted vecfilter precedent; recorded for boundary visibility, not remediation.
- (observation) idiomaticity theodb_rs/src/am/columnar_project.rs:28 — Module-level #![allow(non_snake_case)] appears vestigial — no non-snake identifiers are defined in this module.
- (observation) license_provenance .claude/knowledge-base/references-catalog.md:285 — Seeded external-mention findings (13, lines 44..370) CHALLENGED as NOT license defects: these github.com repo URLs are the project's OWN curated study acervo (references-catalog.md is its own provenance record), each carrying an explicit License field + license-gate decision. The AGPL peers most relevant to M149 — citus, hydra, paradedb — are marked *(study-only)* with 'clone-anyway-study-only — AGPL: barrado no pacote por D1. Só estudo.' The acervo files are gitignored and never in the distribution (CLAUDE.md § Acervo). Systemic per golden §2c — one documented study-acervo policy, not 13 defects.
- (observation) license_provenance CHANGELOG.md:1 — Seeded git-provenance-import findings (vendor(rabitq)/release commits) CHALLENGED as NOT license defects. The rabitq vendor commit declares its license in the subject itself — Apache-2.0 — a permissive, D1-compatible vendoring with attribution; the tool flagged it because it only reads subjects. RaBitQ is also in the acervo as an open-access paper (CLAUDE.md § Acervo). This is pre-existing history, not introduced by the M149 diff (M149 added only the 10-line Added entry at CHANGELOG.md:17-26).
- (observation) license_provenance CHANGELOG.md:478 — Seeded external-mention (vendored) findings CHALLENGED as NOT license defects — each is an honest disclosure of a NON-distributed artifact: HammerDB (GPLv3) is measured via its CLI with 'no HammerDB source vendored/linked' (using a GPL tool's CLI is not linking); the ClickBench 'hits' dataset is 'CC-BY-NC-SA, CI-only, never vendored' (clickbench-ab.json:573); ROADMAP.md:1534 tracks deleting/feature-gating the inert rabitq/vendor/ tree — the project actively managing its OWN vendored hygiene, not an undisclosed import. None enters the Apache-2.0 distribution; all are D1-compliant.
- (observation) maintainability_extensibility theodb_rs/src/am/columnar.rs:775 — decode_columns has measured cyclomatic complexity 27 (McCabe), above 25 — PRE-EXISTING (M100/df_executor read seam, NOT changed by M149). Reported for the maintainability record since the file is fully in the pillar queue; the projection/zone-map/pending branches share one 104-line body on the performance-critical decode path.
- (observation) maintainability_extensibility theodb_rs/src/am/columnar.rs:925 — directory_minmax has measured cyclomatic complexity 28 (McCabe), above 25 — PRE-EXISTING (commit dced43e 'min/max aggregate + zone-map directory fast-path', NOT M149). Reported for the maintainability record because columnar.rs is fully in the pillar queue.
- (observation) maintainability_extensibility theodb_rs/src/am/columnar.rs:1066 — Two column-selection representations coexist in columnar.rs: the scan path uses a per-column `want_mask: &[bool]` (decode_stripe/form_row, absolute-index NULL writing) while the DataFusion path uses `projection: Option<&[usize]>` -> positional `wanted` (decode_columns). load_next_batch bridges scan_projection's `Rc<Vec<usize>>` into a fresh `Vec<bool>` on every stripe. A future third columnar consumer must pick between the two; the divergence is DOCUMENTED and justified (form_row needs absolute indices; decode_columns returns positional), so this is an advisory extensibility note, not a defect.
- (observation) semantic_folders theodb_rs/src/am/columnar_project.rs:22 — am/ module directory holds 23 children (21 .rs files + 2 subdirs page/, hnsw_page/); a cohesive 8-file columnar/HTAP cluster (columnar.rs, columnar_agg.rs, columnar_codec.rs, columnar_project.rs, customscan.rs, df_executor.rs, arrow_cache.rs, zonemap.rs) could eventually be grouped into a columnar/ submodule mirroring the existing page/ and hnsw_page/ subdirs. Pre-existing; M149's columnar_project.rs is added cohesively (correct family, correct name) and does NOT worsen findability.
- (observation) system_design theodb_rs/src/am/columnar_project.rs:218 — Integration/data-flow — addressed: the projection pathlist hook composes without editing customscan.rs (saves PREV_HOOK and calls the vecfilter hook first) and declines aggregate queries (hasAggs/groupClause/groupingSets/hasWindowFuncs) so columnar_agg's find_scan_relid (which matches T_SeqScan, not this CustomScan) and the vectorized-agg path stay intact.
- (positive) design_patterns theodb_rs/src/am/columnar_project.rs:80 — RAII / Scoped-Guard — applied_correctly: ActiveGuard::install(v) swaps the thread-local ACTIVE side-channel slot and its Drop restores the previous value, so a PG ERROR→Rust unwind at the #[pg_guard] boundary mid-ExecScan can never leave a stale projection installed. Correct save/restore (re-entrant), belt-and-braces with the xact/subxact clears.
- (positive) design_patterns theodb_rs/src/am/columnar_project.rs:117 — CustomScan Provider — applied_correctly: PG18 provider protocol implemented as three function-pointer vtables (CustomPathMethods@117, CustomScanMethods@123, CustomExecMethods@128) registered once via RegisterCustomScanMethods@171; the node IS the scan (scanrelid=rel.relid, no children) — the canonical way to see plan->targetlist/qual that a pure TableAM cannot (module doc §Why). Mirrors the prior art (customscan.rs) and Citus ColumnarAttrNeeded (GoF Strategy via C vtable; Fowler/PG provider contract).
- (positive) license_provenance theodb_rs/src/am/columnar.rs:12 — License provenance UPHELD for the M149 changes to columnar.rs (own code). The diff only threads a want_mask:&[bool] parameter through form_row/decode_stripe/load_next_batch and calls columnar_project::scan_projection — all idiomatic Rust over the project's OWN pre-existing decoder; no third-party source introduced. The file's license header (ADR-0042) already declares own code with Hydra+Citus columnar (AGPLv3) study-only and cstore_fdw/arrow-rs (Apache-2.0) as the permissive reuse.
- (positive) license_provenance theodb_rs/src/am/columnar_project.rs:26 — License provenance UPHELD as compliant (own code, Rule 9). The new M149 CustomScan explicitly declares own code and treats Citus columnar (AGPLv3) as design literature only — no source copied, no library linked. Verified as a STUDIED STANDARD, not a port: columns_needed() implements the canonical PG technique (pull_var_clause(targetlist)∪pull_var_clause(qual) → BTreeSet<usize> of 0-based attnos) in idiomatic Rust (Option/Rc/RefCell/thread_local); Citus ColumnarAttrNeeded is C returning a Bitmapset with distinct identifiers (neededColumns/attr_needed) — convergent, not copied. The three Citus references (lines 7, 26, 349) are honest study citations with the AGPL disclosed; no distinctive Citus variable name or ported comment appears; no Cargo.toml dependency links citus/hydra/paradedb.
- (positive) semantic_folders docs/benchmarks/m149-projection-pushdown.md:1 — docs/benchmarks/m149-artifacts/ is the correct home for the raw bench artifacts: it is a verbatim mirror of the established per-milestone convention (m148-artifacts/, m57-raw/, m138-data/, m140-*-data/), with the human-readable verdict m149-projection-pushdown.md living one level up in docs/benchmarks/ exactly like m148's m148-flamegraph-scan.md. Cohesive and findable.
- (positive) semantic_folders theodb_rs/src/am/columnar_project.rs:1 — columnar_project.rs is placed correctly: it sits with its columnar_* family in theodb_rs/src/am/ and immediately alongside customscan.rs, the CustomScan provider it chains after. The name is screaming-architecture (projection pushdown over the columnar TAM), and its mod.rs declaration follows the repo's `M<N> — description` comment convention verbatim. Findable and cohesive.
- (positive) system_design theodb_rs/src/am/columnar.rs:727 — Performance/memory — addressed: masked-out columns skip read_chunked + zstd decode and are never allocated (placeholder Vec::new()), so projected scans materialize O(wanted) column buffers per stripe instead of all 105, and peak memory stays O(one stripe) via the lazy load_next_batch.
- (positive) system_design theodb_rs/src/am/columnar_project.rs:108 — Fault tolerance — addressed: transient projection state is bounded to one (sub)transaction — the Rc<Vec> registry lives Rust-side (not in the palloc0'd ProjScanState so PG frees never skip Drop), the pull-window swap is an RAII ActiveGuard restored even on longjmp, and xact/subxact abort clears orphaned entries + the active slot, closing the scandesc ABA window.
