# Review backlog — develop (diff)

- Iteration: 0
- Verdict: IN_PROGRESS
- Files fully reviewed: 30 / 30
- Open defects: 50 (1 blocking, 49 advisory + 0 systemic group(s))
- Advisory acknowledgements (observation/positive): 12

## Pillar status (derived)

| Pillar | Status | Files done | Open findings | Blockers |
|---|---|---|---|---|
| code | COMPLETED | 30/30 | 12 | 1 |
| architecture | COMPLETED | 30/30 | 3 | 0 |
| system_design | COMPLETED | 30/30 | 5 | 0 |
| design_patterns | COMPLETED | 30/30 | 1 | 0 |
| semantic_folders | COMPLETED | 30/30 | 2 | 0 |
| security | COMPLETED | 30/30 | 5 | 0 |
| tests_deadcode_docs | COMPLETED | 30/30 | 4 | 0 |
| license_provenance | COMPLETED | 30/30 | 25 | 0 |
| idiomaticity | COMPLETED | 30/30 | 2 | 0 |
| maintainability_extensibility | COMPLETED | 30/30 | 3 | 0 |

## Open blocking findings (must fix)

- [BLOCKER] code theodb_rs/src/am/columnar.rs:1186 — accumulate_row muta (*slot).tts_values[i] com flats detoasted e faz pfree antes de retornar; o executor le o MESMO slot apos o insert (RETURNING/AFTER-trigger/index) via slot_getattr sem re-deform -> use-after-free. Convergencia de 2 councils com PG18 source (nodeModifyTable.c 1240/1277/1332). Repro: INSERT INTO c VALUES(1,repeat('x',4000)) RETURNING t. Remediacao: formar o htup de um Vec<Datum> local, slot intacto + teste RETURNING.  (finding 5b2598c12639f2c1)

## Advisory defects (not blocking)

- [MEDIUM] code benchmarks/run_m128_clickbench.py:283 — `sys.exit(main())` but `main()` (line 251) has no `return`, so it returns None and the process ALWAYS exits 0 — even when the byte-identical A/B oracle records `result_ab.diverged > 0`, whose verdict string is literally 'DIVERGENCE — pushdown bug' (run(), line 241). A detected columnar-vs-heap correctness divergence — the very oracle this harness exists to add over ClickBench — therefore does not fail the process, so a CI step chained on exit status proceeds as if the run passed. Violates fail-loud/fail-fast (rules/error-handling.md §1-§2: return explicit errors, do not exit 0 on failure).
- [MEDIUM] license_provenance .claude/knowledge-base/plans/columnar-toast-materialize-plan.md:9 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/plans/m148-flamegraph-spike-plan.md:62 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance CHANGELOG.md:1 — commit history signals third-party origin: 1bf3df0 “vendor(rabitq): core RaBitQ (Apache-2.0) p/ futuro IVF-RaBitQ (Gap 2, ADR-0032)” touched this file
- [MEDIUM] license_provenance CHANGELOG.md:1 — commit history signals third-party origin: 8ec48dd “chore(release): v0.63.0 — HNSW extendCandidates (recall 0.974->0.990) + RaBitQ vendor + veredito P0” touched this file
- [MEDIUM] license_provenance CHANGELOG.md:453 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance CHANGELOG.md:470 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance CHANGELOG.md:485 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance CHANGELOG.md:791 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance CHANGELOG.md:1267 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance ROADMAP.md:1534 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance benchmarks/run_m128_clickbench.py:12 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance benchmarks/run_m128_clickbench.py:25 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance benchmarks/run_m128_clickbench.py:42 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance benchmarks/run_m128_clickbench.py:59 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance benchmarks/run_m128_clickbench.py:245 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance docs/benchmarks/gate-1m-postfix.json:573 — external-project reference (vendored) — verify license/attribution
- [MEDIUM] license_provenance docs/benchmarks/m148-artifacts/m148-scanpuro-flamegraph.svg:4 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance docs/benchmarks/m148-artifacts/m148-slow-flamegraph.svg:4 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] maintainability_extensibility benchmarks/run_m128_clickbench.py:142 — run() is a large multi-responsibility orchestrator: MEASURED ruff PLR0915 = 68 statements (> 50), McCabe CC = 10 (at the default ceiling). One method prepares the dataset, opens the connection, runs the per-query loop, canonicalizes results and builds the report — several reasons to change.
- [MEDIUM] security benchmarks/run_m128_clickbench.py:50 — Detected a dynamic value being used with urllib. urllib supports 'file://' schemes, so a dynamic value controlled by a malicious actor may allow them to read arbitrary files. Audit uses of urllib calls to ensure user data cannot control the URLs, or consider using the 'requests' library instead.
- [MEDIUM] security benchmarks/run_m128_clickbench.py:53 — Detected a dynamic value being used with urllib. urllib supports 'file://' schemes, so a dynamic value controlled by a malicious actor may allow them to read arbitrary files. Audit uses of urllib calls to ensure user data cannot control the URLs, or consider using the 'requests' library instead.
- [MEDIUM] security benchmarks/run_m128_clickbench.py:109 — `_ensure_sample` monta uma string de shell e a executa via `bash -c`, interpolando `path` NAO-quotado na redirecao `> {path}`. `path` deriva do arg CLI `--cache` (`args.cache`), controlado pelo operador — um valor com metacaracteres (espaco, `;`, `$(...)`) quebra o comando ou injeta (CWE-78 / OWASP A03). `_UA` e `HITS_TSV_GZ` sao constantes e `inner` e numerico (int-cast), entao o unico taint e `path`.
- [MEDIUM] tests_deadcode_docs theodb_rs/isolation/columnar_toast_check.sh:116 — EC-4 (limiar de TOAST) afirma exercitar os tres estados 'inline, comprimido inline, externo', mas o fixture t_thr usa repeat('a',1900)/('b',2000)/('c',2100) SEM STORAGE EXTERNAL — valores repetitivos que o TOAST comprime inline e NUNCA externaliza. O estado 'externo' (exatamente o que dispara o #190) nao e alcancado por EC-4; ele so e coberto pelo fixture `src` (STORAGE EXTERNAL). A propria secao de fixture do harness (linhas 54-56) documenta esse fato: 'sem [EXTERNAL] o TOAST comprime primeiro e um valor repetitivo fica INLINE, jamais alcancando a toast table'. Logo o comentario de EC-4 (linhas 116-117) contradiz o proprio harness. A mesma afirmacao falsa se propaga do edge-case review columnar-toast-materialize-edge-cases-2026-07-24.md:70 ('Este teste exercita os tres estados (inline, comprimido inline, externo)'). O #190 permanece coberto pelos testes (1)/(2)/(3), entao nao ha lacuna de regressao real — mas a documentacao executavel de EC-4 sobre-declara cobertura.
- [LOW] code .github/workflows/cassert-sql-safety.yml:21 — The `push` (and `pull_request`) event declares BOTH `paths-ignore:` (line 21 / line 31) AND `paths:` (line 25 / line 35). GitHub Actions does not support both path filters for the same event — when both are present it honours `paths` and ignores `paths-ignore`. Since the existing `paths:` globs (`theodb_rs/src/**`, `Cargo.*`, `scripts/cassert-smoke.sh`, the workflow file) already exclude `.claude/**`, `**/*.md` and `docs/**`, the added `paths-ignore` block is inert: it neither adds nor changes filtering, yet reads as load-bearing. Same redundant/misleading combination in license-gate.yml (19+23), lint-rust.yml (18+22, 28+32) and schema-drift-gate.yml (29+33). (ci.yml and ci-canary.yml correctly use ONLY paths-ignore.)
- [LOW] code benchmarks/profile_columnar_scan.sh:92 — `nsamples=$(perf script ... | grep -c "^[a-z]" || echo 0)` mishandles the zero-match case: `grep -c` prints '0' to stdout AND exits 1 when there are no matches, so `|| echo 0` ALSO runs, making `nsamples` the two-line value `0\n0`. The EC-1 floor gate on line 94 `[ "$nsamples" -ge 500 ]` then hits bash 'integer expression expected' (a malformed integer), not the intended numeric comparison. The gate still fails-closed (the `[` error path triggers `|| fail`), so no false-pass — but the sample-floor is enforced by an accident rather than by the comparison it documents (error-handling / robust exit-status handling).
- [LOW] code benchmarks/run_m128_clickbench.py:153 — Multiple statements on one line (semicolon)
- [LOW] code benchmarks/run_m128_clickbench.py:161 — The heap-copy DDL and A/B query are derived by GLOBAL substring replacement of 'hits': line 161 `create_sql.replace("USING theodb_columnar", "").replace("hits", "hits_heap")` and line 218 `ab_sql.replace("hits", "hits_heap")`. `str.replace` rewrites EVERY occurrence of the substring 'hits', not just the table token, so any identifier/literal containing 'hits' would be silently mangled — breaking the byte-identical A/B correctness oracle. It works only because the current ClickBench schema/queries happen to contain no other 'hits' substring; it is a latent contract fragility, not a guaranteed-correct rewrite (naming/contract; KISS says target the table, not a substring).
- [LOW] code benchmarks/run_m128_clickbench.py:201 — Multiple statements on one line (semicolon)
- [LOW] code benchmarks/run_m128_clickbench.py:217 — Multiple statements on one line (semicolon)
- [LOW] code benchmarks/run_m128_clickbench.py:218 — Multiple statements on one line (semicolon)
- [LOW] code benchmarks/run_m128_clickbench.py:230 — Multiple statements on one line (semicolon)
- [LOW] code theodb_rs/src/am/columnar.rs:1174 — leak Rust-heap do Vec owned/bytes no caminho de longjmp entre alloc e pfree (bounded, backend sobrevive; custo padrao pgrx). A reescrita do F1 nao piora.
- [LOW] code theodb_rs/src/am/columnar.rs:1197 — After the varlena materialisation loop replaces `*value = pg_sys::Datum::from(flat)` in the executor's slot (line 1186), `accumulate_row` frees each `flat` with `pg_sys::pfree` (lines 1196-1198) but never restores the original datums — so `(*slot).tts_values[i]` is left pointing at freed memory, a dangling pointer inside a caller-owned `TupleTableSlot`. It is safe TODAY only because the executor re-materialises the slot (slot_getallattrs) before any subsequent read; any future caller that reads `tts_values` after `columnar_tuple_insert`/`columnar_multi_insert` returns (without a re-deform) would hit use-after-free. Principle: do not leave dangling references in a structure you do not own (memory-safety contract).
- [LOW] idiomaticity theodb_rs/src/am/columnar.rs:1175 — `.take(natts)` is a no-op: `values` is `from_raw_parts_mut(tts_values, natts)` (line 1172), so the slice is already exactly `natts` long; the adapter caps nothing and misleads the reader into thinking the slice could be longer.
- [LOW] license_provenance docs/benchmarks/m148-artifacts/m148-scanpuro-flamegraph.svg:4 — Committed flamegraph SVG embeds FlameGraph's (Brendan Gregg) built-in JavaScript/CSS template (init/search/zoom functions), which is authored code carried under CDDL-1.0 — a third-party licence living inside the Apache-2.0 tree. This is a docs/benchmarks artifact, the per-file attribution IS present, and CDDL is NOT AGPL, so D1 (the project's adopted no-AGPL-in-the-image gate) is NOT triggered and the file is not part of the shipped distribution image. Flagged only for completeness per the extreme-rigor premise; ambiguity: beyond the project's adopted D1 bar.
- [LOW] license_provenance docs/benchmarks/m148-artifacts/m148-slow-flamegraph.svg:4 — Committed flamegraph SVG embeds FlameGraph's (Brendan Gregg) built-in JavaScript/CSS template (init/search/zoom functions), which is authored code carried under CDDL-1.0 — a third-party licence living inside the Apache-2.0 tree. This is a docs/benchmarks artifact, the per-file attribution IS present, and CDDL is NOT AGPL, so D1 (the project's adopted no-AGPL-in-the-image gate) is NOT triggered and the file is not part of the shipped distribution image. Flagged only for completeness per the extreme-rigor premise; ambiguity: beyond the project's adopted D1 bar.
- [LOW] maintainability_extensibility benchmarks/profile_columnar_scan.sh:22 — Self-documentation drift inside the harness: the usage header states the default output dir is docs/benchmarks/, but the code defaults OUT_DIR to /root/m148-out. A maintainer trusting the header looks for artifacts in the wrong place; the two must be kept in sync by hand, which is exactly the changeability trap good self-docs should remove (KISS / DRY of a single fact).
- [LOW] maintainability_extensibility theodb_rs/isolation/columnar_toast_check.sh:5 — A comment hardcodes a source line number (`columnar.rs:206`) to point at the pre-commit flush path. Line-number references in prose are a maintainability fragility: they are not checked by any tool and drift silently as columnar.rs changes, sending a future maintainer to the wrong line. Verified accurate today, but nothing keeps it accurate.
- [LOW] security .github/workflows/schema-drift-gate.yml:57 — `${{ github.event.pull_request.base.sha || github.event.before }}` e interpolado diretamente num bloco `run:` de shell (tambem na linha 84). A guia de hardening do GitHub Actions (CWE-94 script injection) recomenda passar `github.event.*` via `env:` e nunca inline no shell. Aqui o valor e um SHA de commit (hex, validado pelo GitHub), entao NAO e exploravel — defesa-em-profundidade, nao um defeito ativo.
- [LOW] semantic_folders docs/benchmarks/RELATORIO-clickbench-sessao-2026-07-24.md:1 — docs/benchmarks/ is a flat god-folder (223 top-level files); this diff adds 5 more clickbench-session files flat instead of grouping them in a subdir, even though the SAME diff groups m148 output under m148-artifacts/ — an internal cohesion inconsistency.
- [LOW] semantic_folders docs/benchmarks/RELATORIO-clickbench-sessao-2026-07-24.md:1 — File name breaks the folder's lowercase-kebab-case convention: it is the only ALLCAPS-prefixed file in docs/benchmarks/, hurting sort-order/findability among its clickbench siblings.
- [LOW] tests_deadcode_docs benchmarks/profile_columnar_scan.sh:17 — Deriva doc<->codigo: o cabecalho (linha 16-17) e o echo final (linha 124) anunciam saidas que o script NAO produz. (a) `m148-perfstat.txt` e citado nas linhas 17 e 124 mas nenhum comando `perf stat` existe no harness — perfstat foi removido, como o proprio m148-flamegraph-scan.md:68 declara ('O perf stat anexado foi removido do harness por ser fragil'). (b) A linha 16 nomeia `m148-scan-flamegraph.svg`, mas o label usado (linha 111 `profile_query "slow"`) e os artefatos versionados sao `m148-slow-flamegraph.svg`. Documentacao executavel deve descrever as saidas reais.
- [LOW] tests_deadcode_docs docs/benchmarks/m148-flamegraph-scan.md:117 — A tabela 'Self-time por alavanca' (57.4%/22.5%/7.2%...) e as contagens 'Amostras 1440/1087' sao os numeros de decisao do M148 (a priorizacao M149->M151->M150), mas o script Python que classifica os frames folded nos 5 buckets ('alocacao por-linha', 'materializa-row', etc.) NAO esta versionado — a Reproducao (linha 117) remete a 'historico do commit'. Um leitor nao reproduz os percentuais exatos a partir dos folded versionados sem reconstruir o classificador. Fere a norma measurement-first (analysis-golden-rule 'Methodology: exact commands to reproduce every measurement'; public-copy §4). Os folded/SVG estao versionados e o eixo I/O=0% e o `__strcoll_l` so-no-slow foram verificados como fieis aos artefatos — a lacuna e so na etapa folded->buckets.
- [INFO] system_design benchmarks/profile_columnar_scan.sh:90 — Profiling harness data-flow — addressed: the M148 flamegraph harness enforces a >=500-sample floor (EC-1) so an empty capture cannot prioritise the wrong milestone, profiles two representative queries (EC-2) over the REAL 105-column ClickBench schema, and derives the CPU-vs-I/O axis from the captured folded stacks rather than a fragile attached perf-stat. The measured verdict (scan is ~100% CPU-bound, ~80% per-row heap-tuple materialisation, ~7% column decode) corrected the initial code hypothesis by evidence — the correct observability discipline for a hot path.
- [INFO] system_design benchmarks/run_m128_clickbench.py:82 — Benchmark data-flow / measurement honesty — addressed: the sampling path was changed from `head -n` (first-N rows = a narrow temporal slice of the EventTime-ordered dataset, artificially low GROUP BY cardinalities that FAVOUR the vectorized pushdown) to systematic 1-in-k sampling that scans the whole file, at the honest cost of decompressing the full ~100 GB stream. The chosen strategy is recorded in the result artifact (sample_strategy), and `head` is retained only for smoke tests. This removes a bias that inflated the project's own numbers.
- [INFO] system_design theodb_rs/isolation/columnar_toast_check.sh:71 — Data-integrity regression harness — addressed: the #190 fix is guarded by a non-vacuity gate (fails loud if no value actually reached the toast table, so a green-empty run cannot mask the defect) plus md5-of-content assertions across same-txn, cross-txn (EC-6, the pre-commit no-snapshot path), fixed-type integrity (EC-5) and the TOAST threshold (EC-4). This proves crash-safe correctness of the data path, not merely row counts.
- [INFO] system_design theodb_rs/src/am/columnar.rs:1176 — Performance / ingestion throughput — addressed (with disclosed caveat): detoasting every varlena per row at ingestion moves cost from flush into INSERT. Measured A/B on the same box (1M rows, no-TOAST adversarial 2-text-column table): pre-fix ~8.6s -> post-fix ~10.4s = +21%, which marginally CROSSES the plan's own 20% reevaluation trigger (R2). The regression is measured (not presumed), disclosed rather than silenced, and a bounded follow-up is scoped (materialise only VARATT_IS_EXTERNAL || VARATT_IS_EXTERNAL_EXPANDED, skipping inline varlenas) as an owner decision. The essential-complexity trade is justified: the detoast happened anyway, only later.
- [INFO] system_design theodb_rs/src/am/columnar.rs:1206 — Backpressure / bounded write memory (maintenance_work_mem budget) — addressed: with rows now materialised at ingestion, the flush-budget check was correctly moved BEFORE the row is pushed (needs_flush_first), and the pending accounting (p.bytes) now reflects the materialised size rather than an 18-byte pointer. This keeps the pending buffer O(maintenance_work_mem) and prevents a single wide varlena (up to 1 GB) from piling on top of already-buffered rows and turning a loud INSERT error into a backend OOM (edge-case EC-1).

## Observations & positives (advisory — not defects)

- (observation) architecture theodb_rs/src/am/columnar.rs:1147 — SRP/cohesion note: accumulate_row now interleaves varlena materialisation + tuple-forming + a two-point flush decision in one ~90-line unsafe fn whose doc says only 'Accumulate one row'.
- (observation) idiomaticity benchmarks/tests/test_clickbench_sample.py:54 — `import inspect` sits inside the test method body rather than at module top. PEP 8 places imports at the top of the file; a function-local import here buys nothing (no cost/cycle concern) and diverges from the file's own top-level import block.
- (observation) license_provenance CHANGELOG.md:470 — Seeded 'vendored' external-mention findings (L453/470/485/791/1267) ADJUDICATED as non-defects — they are PRE-EXISTING CHANGELOG entries (not in this develop-vs-main diff) that document COMPLIANT provenance and the deterministic scanner keyed on the word 'vendored' in sentences asserting the opposite: HammerDB (GPLv3) is used via its CLI only, no source vendored/linked (correct non-derivative use); ClickBench is CC-BY-NC-SA CI-only never vendored; RaBitQ was Apache->Apache vendoring with LICENSE+VENDORED.md+ADR-0032, since DELETED per ADR-0046.
- (observation) license_provenance benchmarks/profile_columnar_scan.sh:6 — M148 flamegraph harness ADJUDICATED clean: it INVOKES Brendan Gregg's FlameGraph as an EXTERNAL installed tool ($FLAME=/root/FlameGraph, outside the repo — not vendored), cites SOTA blogs/books as study literature, and fetches the ClickBench create.sql at runtime (not copied). Same compliant external-tool-invocation pattern as the HammerDB-via-CLI case.
- (observation) license_provenance benchmarks/run_m128_clickbench.py:11 — Seeded external-mention findings (L12/25/42/59/245) ADJUDICATED as non-defects: the ClickBench dataset (CC-BY-NC-SA) is STREAMED and subsampled at runtime and the ClickBench create.sql/queries.sql are FETCHED at runtime — never vendored into the Apache tree. Verified against git, not just prose.
- (observation) license_provenance theodb_rs/src/am/columnar.rs:12 — Exemplary provenance hygiene: the #190 TOAST-materialisation code is OWN work built on PostgreSQL's public C API (pg_detoast_datum_copy, heap_form_tuple, heap_deform_tuple — PostgreSQL License, Apache-compatible). No copied source, no third-party variable/API namesets from Citus/pg_mooncake/Hydra. The Citus/DuckDB/ClickHouse mentions are design-literature citations, not code copies.
- (observation) security benchmarks/run_m128_clickbench.py:50 — Adjudicacao dos findings semgrep seeded a4a4952a7b84ccc7 (l.50) e a26fa109a1c5605c (l.53) 'dynamic-urllib-use-detected': FALSO-POSITIVO ao bar do projeto. A URL nao e dinamica/atacante-controlada — a base `_CB_RAW` e um literal https constante e a f-string so anexa um caminho estatico (`/queries.sql`, `/create.sql`); o esquema `file://` que o semgrep teme nao e alcancavel. O codigo ja carrega `# noqa: S310`. Beyond adopted bar (golden 2b) -> INFO/observacao, nao-bloqueante.
- (observation) tests_deadcode_docs theodb_rs/isolation/Makefile:37 — O harness de regressao do #190 (`columnar_toast_check.sh`) ganhou o alvo `make check-toast`, mas nenhum workflow em .github/workflows/ o invoca — a esteira (ci.yml) nao roda nenhum alvo de isolation (check-isolation/check-crash/check-corrupt/check-compat/check-toast). Consistente com a convencao documentada do projeto (CI nao roda `cargo pgrx test`; esses harnesses rodam manualmente no droplet), entao NAO e regressao introduzida por este diff — mas o proprio Makefile registra a licao 'um harness sem chamador e uma prova que ninguem executa' (check-corrupt/check-compat). O #190 nao fica CI-gated: uma regressao futura da materializacao TOAST nao seria pega automaticamente. Observacao advisory.
- (positive) architecture theodb_rs/src/am/columnar.rs:564 — DRY (Martin, Rule 12) + SRP done right: 3 duplicated heap-deform loops consolidated into one single-responsibility helper.
- (positive) architecture theodb_rs/src/am/columnar.rs:1153 — Appropriate coupling: detoasting at ingestion removes flush_pending's hidden temporal dependency on an active executor snapshot.
- (positive) design_patterns benchmarks/run_m128_clickbench.py:76 — Strategy (implicit) — applied_correctly: `_ensure_sample(strategy=...)` swaps two interchangeable sampling algorithms (`head` = biased temporal slice vs `systematic` = 1-in-k full scan) at runtime via a minimal if/else. A full class-based Strategy would be over_engineered for two one-line shell-fragment variants (catalog Strategy row: 'Do NOT apply when single algorithm forever' + KISS/YAGNI counterweight).
- (positive) license_provenance .github/workflows/license-gate.yml:1 — Positive: the D1 provenance rule (Apache-2.0 distribution, no AGPL) is enforced by MACHINE via cargo-deny with a fail-closed licence allowlist — an unknown licence FAILS rather than passing. Combined with a comprehensive root NOTICE that reproduces the PostgreSQL-License notices for every redistributed third-party component, the project's provenance posture is strong.
