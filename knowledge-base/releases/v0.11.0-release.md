# Release v0.11.0

**Date:** 2026-06-28
**Verdict:** RELEASED
**Milestone:** M12 — theodb_ai_nl config surface (config/templates/value-index, feature 12)
**Source review:** `.claude/knowledge-base/reviews/m12-nl-surface-review-2026-06-28.md` (READY_TO_MERGE)
**Plan:** `.claude/knowledge-base/plans/m12-nl-surface-plan.md` (`milestone_id: M12`)
**PR:** https://github.com/usetheodev/theo-db/pull/12
**Merge commit:** 59e2c3ea2354ed78ef681cae77edaa6366b6f02d
**Tag:** v0.11.0 (annotated, on the merge commit)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.11.0

## Scope

M12 ships the `theodb_ai_nl` config surface (`ai.nl_config` / `ai.nl_templates` / `ai.nl_value_index` +
`ai.nl_query_cfg` + management fns) over the **unchanged** M7-S4 NL gate (`sql/60` byte-identical). The
config enriches the prompt + supplies allowed-relations; the deterministic L1-L4 anti-injection gate runs on
every query (proven: injection through the config path → 22023 + DB intact; config allowlist reaches L4).
Value-index auto-refresh is injection-proof (allowlist + identifier validation + fixed-shape read). Honest
divergence (ADR D1): the literal 58-function AlloyDB extension is deferred; the 3 core capabilities ship in
schema `ai`.

## Gates

- plan-confidence SHIPPABLE 97.6 · code-quality PASS · review READY_TO_MERGE (after live-verified fixes).
- 34 offline nl tests + 1 real-OpenAI green; `sql/60` unchanged; no new dependency (Rule 9).
- Real evidence: `ai.nl_query_cfg('how many documents are there?', 'rcfg')` → `[{"count": 3}]` (gpt-4o-mini).

## Commits (develop → main)

- `2abcc35` chore(plan) · `a8820e2` feat(m12) · `793f491` fix(m12) · `b129c76` docs(m12) · `61c0b9d` chore(release): 0.11.0
