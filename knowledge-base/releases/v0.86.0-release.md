# Release v0.86.0

**Date:** 2026-07-14
**Verdict:** RELEASED
**Type:** incremental (ad-hoc — M99 work-in-progress, NOT a closed milestone)
**PR:** https://github.com/usetheodev/theo-db/pull/98
**Merge commit:** 98e373e3f104ed2ea4ad028b091387485f21bc11
**Tag:** v0.86.0
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.86.0

## What shipped

Four green M99 slices toward the own-code columnar Table Access Method (`theodb_columnar`), all MEASURED,
full suite 283 tests GREEN / 0 failed:

- Phase A — TAM registers; `CREATE TABLE ... USING theodb_columnar` loads (pgrx 0.19/pg17, 45 callbacks,
  append-only surface). Fix: TAM routine in TopMemoryContext (rd_tableam stored by pointer, no memcpy).
- Phase A2 — metapage + monotonic row_number/stripe_id reservation (1000 gap-free).
- Phase B/C1 — write path + reader: INSERT 5001 rows → SELECT result-identical.
- zstd — 20000-row stripe < HALF the heap on-disk size.

Governance: ADR-0042 (own-code; Hydra columnar is AGPLv3, corrected from the M98 amendment's Apache-2.0
mislabel; study design only, Rule 9). Supersedes ADR-0041's DEFER for the own-code path.

## Roadmap checkbox — intentionally NOT flipped

M99's DoD is NOT complete (column-major encoding, min/max skip-pruning, snapshot-scoped cross-backend MVCC,
isolation permutation proofs, crash-safety WAL-replay, and the formal benchmark are follow-up phases). Flipping
the M99 checkbox here would be over-claiming (Rule 3). This is an incremental release of in-progress work; the
M99 milestone stays `[ ]` until its DoD is met. No `milestone_id`-driven flip (ad-hoc release, per
cycle-release § Post-merge checkbox flip WARN path).

## Honest boundary (Rule 3/5)

Append-only analytical columnar TAM with measured compression. No performance-superiority claim. NOT yet a
full columnar engine.
