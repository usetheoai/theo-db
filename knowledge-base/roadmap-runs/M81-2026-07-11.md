---
milestone_id: M81
slug: m81-ivf-aq-lifecycle
date: 2026-07-11
status: completed
release: v0.70.0
checkbox_flipped_at: 2026-07-11
---

# Milestone M81 — lifecycle transacional do índice IVF-AQ v4

## Outcome

INSERT + VACUUM corretos no índice `theodb_ivfflat WITH (pq_subspaces)` (v4). O `scan_ivf_aq` folda a região
pending (rows pós-build, f32, exact-scored — antes perdidas silenciosamente); `main_index_pages`/`read_pending`
v4-aware. VACUUM = safe no-op no v4 (`vacuum_rebuild` gate — o rebuild f32 rejeitaria `read_ivf_meta(v4)`;
correção via fold do pending + MVCC re-check). `amcostestimate` v4-aware. Provado: `ivf_aq_v4_folds_post_build_inserts`
+ 236 pg_tests GREEN, zero regressão. **Honesto:** compactação/rebuild das v4-lists (dead-tuple + pending fold no
disco) = follow-up (REINDEX hoje); correção transacional holds. Release v0.70.0.
