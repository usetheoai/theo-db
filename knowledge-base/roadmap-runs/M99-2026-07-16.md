---
milestone_id: M99
slug: m99-columnar-tam-append-only
date: 2026-07-16
status: completed
plan: knowledge-base/plans/m99-columnar-tam-append-only-plan.md
implementation: knowledge-base/implementations/m99-columnar-tam-append-only-implementation.md
review: knowledge-base/reviews/m99-columnar-tam-append-only-review-2026-07-16.md
release: knowledge-base/releases/v0.87.0-release.md
checkbox_flipped_at: 2026-07-16T00:00:00Z
flip_commit_sha: 7c7cdfe57c8ffb2e67cd816369548365b64d0ead
---

# Milestone M99 — columnar TAM append-only (own-code)

## Objective (from ROADMAP.md)

An own-code columnar TableAM (`theodb_columnar`, ADR-0042 — Hydra columnar is AGPLv3, design study only, Rule 9):
stripe (10k chunk-group) → per-column chunk + per-column compression (zstd) + min/max skip directory; synthetic
row_number TID; WAL via GenericXLog (reuses `am/page.rs`); MVCC append-only (stripe visible ⟺ catalog row visible).
Honest append-mostly scope: no update-in-place, no parallel/bitmap/sample.

## Definition of done (from ROADMAP.md)

1. `theodb_columnar` TAM registrable (`CREATE ACCESS METHOD ... TYPE TABLE`) — ✅
2. result-equivalence pg_tests vs row-store — ✅
3. pg_isolation permutation specs (MVCC) green + isolationtester wired — ✅
4. crash-safety WAL-replay — ✅
5. benchmark columnar vs heap (compression measured; skip/vectorization = M100, honest) — ✅
6. sign-off council-index-storage + council-rust-pgrx — ✅

## Outcome

Shipped in v0.87.0 across 8 commits (C1 column-major, C2a MVCC catalog, C2 DROP cleanup, D1 isolation, D2 crash+
benchmark, review corrections). 297 pg_tests + 3 isolation permutations + crash-safety GREEN on the droplet; 9.2×
on-disk compression measured; all DoDs met; 3-council READY_TO_MERGE. Scan-speed gain (projection/skip/vectorization)
is honestly deferred to M100. Follow-up issues #99 (WRITE_STATES OOM) + #100 (estimate_size) filed.
