---
milestone_id: M65
slug: m65-rerank
date: 2026-07-09
status: completed
plan: .claude/knowledge-base/plans/m65-rerank-plan.md
implementation: theodb_rs/src/rerank.rs + api.rs + lib.rs (14 pg_test) + benchmarks/run_m65_rerank.py
review: council-benchmark HONESTO (honest-negative legítimo, idx-mapping verificado, sem bug)
release: knowledge-base/releases/v0.55.0-release.md
checkbox_flipped_at: 2026-07-09
flip_commit_sha: pending-release-commit
---

# Milestone M65 — Reranking own-code (ai.rerank)

## Outcome
ai.rerank(query, docs[]) RETURNS TABLE(idx, score) — own-code Rust (rerank.rs espelha embed.rs::run_batch,
reusa http.rs::post_json, Regra 9; parser N-in/N-out do shape cross-encoder Cohere/BGE/TEI). 14 #[pg_test]
GREEN (guards/SSRF/parser). Benchmark BEIR/SciFact (100 queries, 3 runs determinísticos): baseline nDCG@10
0.7327 vs rerank BGE-reranker-base 0.6947 → delta −0.038 HONEST-NEGATIVE (o rerank PIOROU), Recall@50
conservado 0.92 (sanity), custo ~1.96s p50. Exatamente o previsto pela literatura (cross-encoder off-the-shelf
regride em corpus fora de distribuição — SciFact é fact-checking científico). council-benchmark: HONESTO
(idx-mapping verificado ponta-a-ponta, sem bug — honest-negative legítimo). DECISÃO (DoD exige): ai.rerank
embarca (superfície model-agnostic correta/testada/medível), NÃO se afirma ganho de qualidade (public-copy §4),
rerank é opt-in (o operador escolhe o reranker adequado ao seu corpus por GUC). LIÇÃO: benchmark CPU cross-encoder
sobre corpus de abstracts longos é lento (~2s/rerank de 50 docs); rerank determinístico → 1 run basta p/ o delta.
