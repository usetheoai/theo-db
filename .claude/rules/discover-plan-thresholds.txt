# Discover-plan-confidence verdict bands.
#
# Formato CANÔNICO: pipe-delimitado `NAME|threshold|sunset|adr` — lido por
# `skills/discover-plan-confidence/scripts/run_discover_plan_score.py` (_parse_thresholds usa split("|")
# e _verdict_for retorna o próprio NAME como token de verdict). Espelha `discover-blueprint-thresholds.txt`.
# Bands apertados (frontier) por ADR 0001-discover-phd-rigor § 3.1.
#
# Os HARD CAPS (citação fabricada, corner vazio, budget de perguntas) NÃO são lidos daqui — são enforced
# pelos checker scripts (check_reference_citations / check_research_coverage / check_plan_completeness).
#
# Correção (2026-06-27): o arquivo estava no formato `band.x = N` (equals), incompatível com o parser
# pipe-delimitado → bands vazio → verdict colapsava para INVALID independente do score. Migrado ao formato
# canônico (mesmas bandas 92/75/50/0; mesmos tokens do golden rule + exit codes do runner).
SHIPPABLE|92|2027-01-31|0001-discover-phd-rigor
SHIPPABLE_WITH_CAVEATS|75|2027-01-31|0001-discover-phd-rigor
NON_SHIPPABLE|50|2027-01-31|0001-discover-phd-rigor
INVALID|0|2027-01-31|0001-discover-phd-rigor
