# Languages enabled for /code-quality and post-edit-check.sh.
#
# Format: one language identifier per line. Lines starting with '#' are comments.
# Identifiers match the file extensions handled by hooks/post-edit-check.sh.
#
# Default policy: empty file = no language-specific quality checks run. Each
# project enables the languages it actually uses. Adding a language here is a
# signal to /code-quality and the post-edit hook that the toolchain should be
# present and consulted.
#
# Supported identifiers (uncomment the ones your project uses):
#
# go
# python
# typescript
# javascript
# rust
# java
# kotlin
# ruby
# csharp

# B-030 (2026-08-12) — habilitado. O arquivo estava INTEIRAMENTE comentado, e a consequência não era
# "nenhuma verificação": era `/code-quality` devolvendo PASS com `languages_audited: []` — um verde que
# não mediu nada, visualmente idêntico a um verde real. É a mesma classe de defeito que o acervo do
# projeto registra como `cobertura-alegada-sem-execucao`, e a que este ciclo já encontrou em duas outras
# formas (o harness de upgrade do B-028 e a imagem de teste com fonte embutida).
#
# `theodb_rs/Cargo.toml` é o único manifesto de código do repositório desde que benchmarks/ saiu.
#
# FORMATO: o cabeçalho acima diz "one language identifier per line" e ISSO ESTÁ ERRADO — o parser
# (`_registry.py`) exige o formato em pipes de `skills/code-quality/defaults/languages.txt`, e um
# identificador solto aborta com `languages.txt malformed line`. Quem seguir a instrução do próprio
# arquivo recebe erro. Corrigir o cabeçalho é mudança na skill, fora do escopo deste ciclo; fica
# registrado aqui e no CHANGELOG para não se perder.
rust       | theodb_rs/Cargo.toml | ENABLED | pgrx extension — o único manifesto de código do repo.
