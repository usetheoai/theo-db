# Release v0.1.0

**Date:** 2026-06-27
**Verdict:** RELEASED
**Source review:** `.claude/knowledge-base/reviews/vector-recall-benchmark-harness-review-2026-06-27.md` (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/2 (MERGED)
**Merge commit:** `0638f9f3fd61d1ebdca1b9a7a3a1597bd5592d8b`
**Tag:** `v0.1.0` (annotated, on the merge commit)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.1.0

## Scope

First release of TheoDB. Cuts the accumulated work on `develop` since project inception (22 commits):

- **M0 Walking Skeleton** — `Dockerfile` (postgres:17 + pgvector 0.8.3, pinned), `smoke.sh`, ADR 0001 (no engine fork).
- **Strategy** — ADR 0002 north-star (igual-ou-superior ao AlloyDB, Opção α) propagated across canon docs.
- **Docs** — `docs/features/` (12 spec pages), `docs/notebook/` (vector store notebook + `theodb` SDK façade).
- **Discovery** — `alloydb-vector-ai-implementation` blueprint (SHIPPABLE 98.7).
- **Harness** — `benchmarks/theodb_bench` recall@k benchmark (the M2 measurement-first gate); first measured evidence at `docs/benchmarks/2026-06-27-pgvector-l2.json`.
- **Tooling fixes** — `discover-plan-thresholds.txt` format + `check_evidence_citations.py` `.claude/` path bug.

## Roadmap checkbox flip (Step 7.5)

**SKIPPED (WARN)** — the source plan `vector-recall-benchmark-harness` has **no `milestone_id`** (it is a *slice* of M2, not the whole milestone). No ROADMAP checkbox was flipped. Single-flip invariant honored (zero flips).

## Follow-up (honest)

M0's deliverables (Dockerfile/smoke/ADR) shipped in this release, but `ROADMAP.md ## M0 — [ ]` is still unchecked because this release was driven by the harness slice, not the M0 plan. The M0 checkbox should be flipped to `[x]` separately (via an M0-scoped release or a manual roadmap-run-recorded flip) to keep the roadmap honest.

## Release notes

See the GitHub release (rendered from `CHANGELOG.md ## [0.1.0]`): the harness, M0, strategy ADR 0002, docs/features + notebook/SDK, the AlloyDB discovery, the PhD-rigor discover profile, and the tooling fixes.
