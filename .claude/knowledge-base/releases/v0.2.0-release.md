# Release v0.2.0

**Date:** 2026-06-27
**Verdict:** RELEASED
**Milestone:** M2 — Vetorial / IA (all 4 DoDs)
**Source review:** `.claude/knowledge-base/reviews/m2-vetorial-ia-review-2026-06-27.md` (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/3
**Merge commit:** 7e88ad9e726c1f07b20228df3d49ce328210c09b
**Tag:** v0.2.0 (annotated, points at the merge commit)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.2.0

## Scope

M2 Vetorial/IA — all four DoDs shipped:

- **DoD-1** — recall@k/latency/QPS/build/memory harness + real ANN-Benchmarks dataset loader (`glove-25-angular`) + CI workflow. Evidence: `docs/benchmarks/2026-06-27-glove-25-angular.json`.
- **DoD-2** — pgvectorscale StreamingDiskANN available in the image; **HNSW chosen as default by real-data evidence** (`docs/decisions/m2-index-decision.md`). DiskANN retained, `UNBENCHMARKED` for the high-dim/large-scale regime.
- **DoD-3** — `theodb.embed()` SQL function over a configurable model endpoint (AlloyDB pattern); `tools/embedding_server.py` real local model. Doc: `docs/sql-embeddings.md`.
- **DoD-4** — Fork Policy (D3) honored — pgvectorscale as-is, commit `57c88b7`, no fork.

## Post-release obligations (tracked)

1. **CI first green run** — the push to `develop` triggered the first real CI run (`.github/workflows/ci.yml`). The ROADMAP `### M2` checkbox flip is held until that run is confirmed green (DoD-1 "rodando em CI"). The plan carries no `milestone_id`, so the automated Step 7.5 flip skips — the flip is performed manually + auditable (roadmap-runs file) once CI is green.
2. **D1 license sweep** — `cargo-deny`/`loop-check-licence` over `vectorscale.so`'s Rust crate tree before distributing the image binary (PRD §11).

## Release notes

See the GitHub release and `CHANGELOG.md` § [0.2.0].
