# Release v0.157.0

**Date:** 2026-07-27 · **Verdict:** RELEASED · **Milestone:** M166 (string aggregates + wide `SUM(expr)` — q29)
**Source review:** `.claude/knowledge-base/reviews/m166-string-agg-review-2026-07-27.md` (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/214 · **Merge commit:** 5955c1dadcba11c5e79149b2c46aca45d8de5786
**Tag:** v0.157.0 · **GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.157.0
**Roadmap flip:** M166 `[ ]`→`[x]` in commit 8fe8a3e0f8d2cc3ca8ce18a495eeedd218f804c2

## Scope

- **M166** (the milestone): wide `SUM(int2_col ± const)` aggregate-argument routing in the columnar-agg CustomScan, so
  ClickBench **q29** (90× `SUM(ResolutionWidth + k)`) routes. New `AggSpec::SumIntAddConst` (kind 9, delta hi/lo-encoded),
  **fail-closed** to a provably-overflow-free class (int2 base + int4 result, whole domain ± delta inside int4); int4/int8
  base, int8 result, non-additive op, out-of-range delta decline. `EXPLAIN VERBOSE` deparse rebuilds the nested Var
  (M131 OUTER_VAR). Measured (1M ClickBench, same-box vs ClickHouse 26.8.1): **q29 567.13× → ≈ 2.10×** (same-engine
  27.79 s → 0.1027 s, ≈ 270×), **43/43 A/B byte-identical (0 regressions)**, type-coverage A/B **31/31** (fail-closed cases
  proven), positive control diverged=2. q21/q22 (`MIN(text)`) recorded as collation honest-negatives; q27 (`AVG(length)`)
  deferred. Review hardening (council-rust-pgrx MEDIUM): builtin-operator guard (`opno >= FirstNormalObjectId`) added to
  both int-arith gates.
- **Release tooling fix:** `flip_milestone_checkbox.py` header regex now matches `##`/`###` (ROADMAP uses two hashes) — the
  M166 flip is deterministic.

## Gates

- `/review` (council-rust-pgrx + council-benchmark): READY_TO_MERGE — no BLOCKER/HIGH; both MEDIUMs + all LOWs fixed
  (commit 4016d98) and re-validated on the droplet (type-coverage 31/31, q29 A/B diverged=0).
- `/code-quality`: FAIL_SOFT, HARD=0 — env-only soft caps (`auditor_unavailable_cargo-udeps` + `symbol_fab_unverifiable_rust`,
  a documented Rust-D2 v0.2 deferral). Dismissed with substitute evidence: the clean droplet compile proves symbol
  resolution + dead-code absence (cargo warns on `dead_code`), and council-rust-pgrx verified the encode→decode→executor
  wiring. No real defect found.
- Benchmark: `docs/benchmarks/m166-wide-sum-verdict.md` — q29 pushdown, 43/43 A/B clean, provenance + caveats folded in.

## Known-red CI (documented, pre-existing — not M166 regressions)

`migration-smoke` (#206) + `publish` blocked by real `gosu` CVEs (#207). Source release + tag valid; image publish blocked
by #207 by design. CI-green is a soft pre-condition; flagged in the PR body.

## Merge

Self-merged PR #214 (Rule 4 — via PR develop→main, authorized by the owner's goal "DEVE FAZER O MERGE VOCE MESMO").
