# Release v0.17.0

**Date:** 2026-06-29
**Verdict:** RELEASED
**Source review:** .claude/knowledge-base/reviews/audit-remediation-review-2026-06-29.md (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/19
**Merge commit:** a141a7cd87a78bc1b33f98e34aabf7d88ec3cdf8
**Tag:** v0.17.0 (annotated, on the merge commit)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.17.0
**ROADMAP flip:** SKIPPED — plan `milestone_id: none` (off-roadmap, audit-driven remediation; per cycle-release WARN-not-block).

## Scope (audit-remediation)

System-Design audit remediation — all 5 actionable "Top Refactor Priorities" + 2 ADRs:
- T1.1 `theodb.embed_batch(text[])` — collapse the CRITICAL embed N+1 to one round-trip (benchmark N→1: N=8 2.93×, N=32 7.49×, N=128 7.99×).
- T2.1 fail-fast seam guard for `theodb.embed` in `ai.hybrid_search_rrf` (0A000).
- T3.1 bounded recoverable-class retry (embed client + `ai._chat`) with observability.
- T4.1 conditional retirement migration of the legacy plpython3u `theodb.embed` + `default_version` 1.1.
- T5.1 `theodb.import_pinecone_chunked` PROCEDURE (per-batch COMMIT).
- T0.1 ADR 0007 (sync per-row HTTP) + 0008 (no cache) → Accepted.

## Evidence

41/41 embed/AI + install + retirement tests green vs rebuilt `theo-db:audit-rem`; `cargo clippy -- -D warnings` clean; `ruff` clean; `/code-quality` PASS; benchmark report at `docs/benchmarks/audit-remediation-embed-batch.md`.
