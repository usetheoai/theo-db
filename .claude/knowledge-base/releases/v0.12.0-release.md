# Release v0.12.0

**Date:** 2026-06-28
**Verdict:** RELEASED
**Milestone:** M13 — native packaged AI surface (`ai.hybrid_search()` JSON + `theodb_ml` registry, specs 06/07)
**Source review:** `.claude/knowledge-base/reviews/m13-packaged-ai-surface-review-2026-06-28.md` (READY_TO_MERGE)
**Plan:** `.claude/knowledge-base/plans/m13-packaged-ai-surface-plan.md` (`milestone_id: M13`)
**PR:** https://github.com/usetheodev/theo-db/pull/13
**Merge commit:** aa3757084a441748adfe93de8a59640a0b2da5e6
**Tag:** v0.12.0 (annotated, on the merge commit)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.12.0

## Scope

M13 ships the literal spec-06/07 packaged surface over existing capabilities (Rule 9 — `ai.hybrid_search_rrf`
+ `ai._chat` unchanged): `ai.hybrid_search(jsonb)` (thin JSON wrapper, parity-tested honest sugar) and the
`theodb_ml` model registry (`create_model`/`apply_model` over named endpoint+model configs). **Security (ADR
D2): API keys are never persisted** — the registry has no `api_key` column; keys stay session GUCs;
`apply_model` bridges endpoint+model to the unchanged `ai._chat`. All new fns REVOKE FROM PUBLIC.

## Gates

- plan-confidence SHIPPABLE 95.2 · code-quality PASS · review READY_TO_MERGE (after live-verified fixes:
  apply_model stale-model GUC reset + test hygiene).
- 9 theodb_ml + 2 hybrid-parity + 1 real-OpenAI green; 103 offline suite; `sql/50` unchanged; `sql/40`
  additive only; no new dependency (Rule 9). Real evidence: registry-applied model → `ai.generate` → `'ok'`.

## Commits (develop → main)

- `fcfee17` chore(plan) · `156433d` feat(m13) · `89c6d76` fix(m13) · `7de4284` docs(m13) · `2ac3595` chore(release): 0.12.0
