# Release v0.155.0

**Date:** 2026-07-27
**Verdict:** RELEASED
**Milestone:** M164 (benchmark harness hardening)
**Source review:** `.claude/knowledge-base/reviews/m164-benchmark-harness-hardening-review-2026-07-27.md` (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/210
**Merge commit:** db5489aa43e8886447f90560442a30e6b5382a74
**Tag:** v0.155.0 (annotated)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.155.0

## Scope

Single-trunk release cutting everything on `develop` since v0.154.0:

- **M164** (the milestone): three false-green/infra guards on `benchmarks/run_m128_clickbench.py` — B1 sample-count
  integrity (`sample_is_fresh`, clamped to `HITS_TOTAL_ROWS`), B2 agg-specific A/B routing (`plan_shows_agg_pushdown` +
  `classify_ab` + `decide_ab_verdict`), C pre-flight sizing (`preflight_sizing`, full-footprint disk BLOCK / RAM WARN).
  Pure env-injected helpers, 20 unit tests, ruff-clean. Reviewed by council-benchmark + cross-validation (round 1
  NEEDS_FIXES → HIGH-1/HIGH-2/2×MEDIUM fixed → round 2 both READY_TO_MERGE).

## Known-red state at release (documented, pre-existing — not M164 regressions)

Same as v0.154.0: `migration-smoke` red (pgvector `ivfflat` drop-in gap, #206); `publish` blocked by 15 `gosu` CVEs
(#207). Source release + tag are valid; the image publish stays blocked by #207 by design.

## Note — concurrent-agent develop divergence at flip time

The post-merge M164 checkbox flip was delayed by a concurrent agent rebasing local `develop` (CI perf work), which
wiped the working-tree flip. The release itself is intact (tag v0.155.0 on merge commit db5489a; GitHub release
published; `origin/develop` carried the release commit). The flip was re-applied and reconciled via a merge (no
force-push, no reset — Unbreakable Rule 4).

## Gates

- `/review`: READY_TO_MERGE (council-benchmark + cross-validation, round 2 sign-off after HIGH fixes).
- `/code-quality`: FAIL_SOFT HARD=0 (env-only; M164 touches no Rust).
- Tests: `benchmarks/test_m164_harness_guards.py` — 20 green, ruff-clean.
- Merge: self-merged PR #210 (Unbreakable Rule 4 — via PR, authorized by the owner's goal).
