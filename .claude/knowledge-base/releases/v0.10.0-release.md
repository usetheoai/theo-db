# Release v0.10.0 (M10 + M11 — bundled)

**Date:** 2026-06-28
**Verdict:** RELEASED
**Milestones:** M10 (`ai.agg_summarize`, feature 11) + M11 (`ai.generate_batch`, feature 08)
**Source reviews:**
- `.claude/knowledge-base/reviews/m10-agg-summarize-review-2026-06-28.md` (READY_TO_MERGE)
- `.claude/knowledge-base/reviews/m11-ai-batch-review-2026-06-28.md` (READY_TO_MERGE)
**Plans:** `m10-agg-summarize-plan.md` (`milestone_id: M10`) · `m11-ai-batch-plan.md` (`milestone_id: M11`)
**PR:** https://github.com/usetheodev/theo-db/pull/11
**Merge commit:** 99b52774c2c17fbc13eff0def7fca4ffcbd2d606
**Tag:** v0.10.0 (annotated, on the merge commit)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.10.0

## Single-flip exception (operator-approved)

Both M10 and M11 were `READY_TO_MERGE` and sat in `[Unreleased]` (M10 was never released on its own).
The operator explicitly chose a **bundled release** over two sequential releases. Per that decision, this
one release flips **TWO** ROADMAP checkboxes (M10 + M11) and writes **two** roadmap-runs files — a
documented exception to the `cycle-release` single-flip invariant. Each flip was performed by
`flip_milestone_checkbox.py` invoked once per milestone (each invocation flips exactly its own checkbox,
honoring the per-invocation single-flip guard) and recorded with the same flip commit SHA.

## Scope

- **M10 — `ai.agg_summarize(text)`**: a PostgreSQL aggregate collapsing many rows into one summary via
  `ai._chat` (no new dep). Real gpt-4o-mini evidence; finalfunc VOLATILE; REVOKE FROM PUBLIC.
- **M11 — `ai.generate_batch(text[]) -> text[]`**: answers N prompts in ONE `ai._chat` round-trip
  (JSON array, len==N + string-element validation, fail-fast). Acceleration measured (batch=+1 vs N
  scalar=+N requests); real gpt-4o-mini evidence `['Paris','4','cold']`.

## Gates (both milestones)

- plan-confidence: M10 SHIPPABLE 96.0 · M11 SHIPPABLE 90.4
- code-quality: PASS (both) · review: READY_TO_MERGE (both, after live-verified fixes)
- 33 offline ai tests + 2 real-OpenAI tests green; ruff clean; no new dependency (Rule 9); backward compat.

## Commits (develop → main)

- `c0f032d` chore(plan) M10 · `ba7052b` feat(m10) · `5cb790e` fix(m10) · `a90eb34` docs(m10)
- `cb02fbd` chore(plan) M11 · `caa3c1f` feat(m11) · `6972fbd` fix(m11) · `73a0e79` docs(m11)
- `1a19661` chore(release): 0.10.0

## Note — version computation

`compute_next_version.py` relies on `git describe --tags` which fails in this develop→main flow; the next
version was computed from the highest existing tag (v0.9.0) → v0.10.0. Follow-up: harden the script.
