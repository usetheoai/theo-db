# Release v0.3.0

**Date:** 2026-06-28
**Verdict:** RELEASED
**Milestone:** M3 — Migração mínima (all 3 DoDs) + a non-milestone fix
**Source review:** `.claude/knowledge-base/reviews/m3-minimal-migration-review-2026-06-27.md` (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/4
**Merge commit:** fe9a735ab0abc6164d7c333789b324e7068bb32c
**Tag:** v0.3.0 (annotated, points at the merge commit)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.3.0
**CI:** run 28319401284 — `harness-unit` + `image-and-bench` + `migration-smoke` all success.

## Scope

- **M3 — Minimal migration (vanilla PostgreSQL → TheoDB)** — 3/3 DoDs. Standard `pg_dump`/`pg_restore`
  (Rule 9, no bespoke tool). `migrate-smoke.sh` (+ selftest + doc-check) proves data + indexes preserved;
  guide at `docs/migration/minimal-migration.md`; CI job `migration-smoke`.
- **Fixed (not M3): `theodb.embed` reaches HTTPS cloud providers** — added `ca-certificates` to the image so
  `plpython3u`'s TLS verification succeeds; validated against the real OpenAI API (`text-embedding-3-small`,
  `vector(1536)`).

## Roadmap

ROADMAP `### M3` flipped `[ ]` → `[x]` (auditable; plan has no `milestone_id` → manual flip recorded in
`.claude/knowledge-base/roadmap-runs/M3-2026-06-28.md`, flip commit `dea78cc`).

## Release notes

See the GitHub release and `CHANGELOG.md` § [0.3.0].
