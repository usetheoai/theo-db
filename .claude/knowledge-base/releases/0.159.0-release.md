---
version: 0.159.0
date: 2026-08-11
cycle: release
input_verdict: READY_TO_MERGE_WITH_FOLLOWUPS
---

# Release 0.159.0

## Verdict de entrada

`cycle-review` emitiu `READY_TO_MERGE_WITH_FOLLOWUPS`
(`.claude/knowledge-base/reviews/b015-review-2026-08-11.md`) — sem BLOCKER, com o achado HIGH
registrado como item de backlog (`B-024`), conforme o hard gate desse verdict.

## Versão — a derivação automática foi RECUSADA, e por quê

| | |
|---|---|
| Última tag | `v0.158.0` |
| Derivação automática do `cycle-release` | **MAJOR** → `v1.0.0` (regra: `Removed` não-vazio ⇒ major) |
| Cortada | **MINOR** → `v0.159.0` |
| Quem decidiu | owner, 2026-08-11, consultado explicitamente |

O `[Unreleased]` continha duas seções `Removed` reais — o access method `theodb_symqg` (M176) e a árvore
`docs/`. A regra deriva major disso e produziria `v1.0.0`.

**Isso colidiria com um gate inquebrável.** O `dogfood-golden-rule` hard cap #2 (`anchor_not_running`)
aceita **apenas** `running`, e o cenário-âncora `theo-rag-sobre-theodb` está `planned`. O README declara,
em texto, que o projeto não é production-ready. Cortar `1.0.0` teria alegado por **acidente de regra** o
que o projeto se recusa a alegar por **evidência** — exatamente a inversão que o dogfood existe para
impedir.

A regra de derivação não distingue `0.x`, onde o [SemVer §4](https://semver.org/) estabelece que nada é
declarado estável e remoções não forçam major. **Essa é uma lacuna da regra**, e ela fica registrada aqui
em vez de ser contornada em silêncio: enquanto o projeto estiver em `0.x`, a derivação automática
proporá major toda vez que houver `Removed`, e alguém terá de recusá-la de novo. Corrigir isso é trabalho
de regra, não deste release.

## Conteúdo

120 entradas, acumuladas desde `v0.158.0` (2026-07-29). Deste ciclo:

- **B-015** — o caminho de scan default não reportava métrica (`explain_scan`/`scan_stats`/coletor liam
  zero para todo índice V1), e os três testes colunares mediam o vazio. Duas causas distintas, ambas
  provadas por experimento de variável única.
- Baseline de falhas da suíte no CI: **10 → 6** (medido: `434 passed; 6 failed`, 539,43 s).

## Gates mecânicos

| Gate | Resultado |
|---|---|
| Suíte `cargo pgrx test pg18` | 434 pass / 6 fail — igual ao baseline; nenhuma falha nos módulos tocados |
| `cargo fmt --check` (arquivos tocados) | limpo |
| Clippy `-D warnings` via `.clippy_args` | **REPROVOU — exit 101, 4 erros, TODOS pré-existentes** (ver abaixo) |
| `stop-validation.sh` | sem BLOCKER |
| `public-copy-lint.sh` | exit 0 |
| Validador OKF da wiki | 300 conceitos, 0 erros, 0 links quebrados, 0 órfãos |

## O gate de clippy reprovou, e a distinção importa

`cargo clippy --features pg18 --no-deps` com o baseline `.clippy_args`: **exit 101, 4 erros**.

**Nenhum deles está nos arquivos alterados por este ciclo.** São dois lints, ambos ausentes do baseline:

| Lint | Local | Natureza |
|---|---|---|
| `clippy::needless_ifs` | `src/am/options.rs:447` | **defeito real** — `if rd_options.is_null() { }` com corpo vazio, seguido de desreferência. Ver `B-026` |
| `clippy::implicit_saturating_sub` | `src/am/df_executor.rs:49` | idioma — o código está correto (guarda `> 0` explícita) |

Duas coisas foram descobertas ao executar o gate, e a segunda só apareceu porque a primeira foi resolvida
em vez de contornada:

1. **A imagem `theodb-builder` não traz `cargo-clippy`** (`B-025`). A primeira execução devolveu exit 1
   por ferramenta ausente — que é trivialmente confundível com "lint reprovou". O golden rule manda
   registrar `auditor_unavailable_{tool}` e **nunca fabricar saída limpa**; o componente foi instalado no
   contêiner para que o gate rodasse de verdade.
2. **O gate já estava vermelho** (`B-026`), com dívida anterior a este ciclo. Um gate permanentemente
   vermelho não protege nada: o próximo defeito real entra sem ser barrado, porque vermelho virou o
   estado normal.

**Consequência para este release:** o `cycle-release` lista como anti-pattern cortar release com
`FAIL_HARD` de code-quality em aberto. Os findings são pré-existentes e o release carrega 120 entradas
acumuladas — o repositório já estava assim quando elas foram escritas. A decisão de cortar mesmo assim
é do owner, e fica registrada aqui em vez de ser tomada em silêncio por quem executou o ciclo.

## Itens de backlog abertos neste ciclo

`B-020` (build HNSW 93× mais lento que inserção), `B-021` (diagnóstico cego ao shim pgvector),
`B-022` (`#[pg_test(error=)]` com fragmento), `B-023` (teste de performance na suíte funcional),
`B-024` (autotune sobre zeros — followup HIGH do review). `B-019` foi **morto** com controle medido;
`B-018` segue aberto e não reproduzido em 6 cenários.
