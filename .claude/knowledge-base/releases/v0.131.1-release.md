# Release v0.131.1

**Date:** 2026-07-22
**Verdict:** RELEASED
**Tipo:** housekeeping (patch) — **sem mudança de produto** (`theodb_rs/`/`sql/` diff vazio)
**Source review:** nenhum cycle-review formal (declarado honestamente no PR). Revisão ad-hoc desta sessão: `/review @benchmarks/` = saudável, 0 BLOCKER; `fix(build)` do Makefile validado por paridade com o Dockerfile.
**PR:** https://github.com/usetheodev/theo-db/pull/161
**Merge commit:** 7921770c8b77c1f7c4e3b0d2093a67feb9a6da7f
**Tag:** v0.131.1 (anotada)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.131.1
**ROADMAP checkbox:** N/A — plan sem `milestone_id` (housekeeping off-roadmap); flip pulado com WARN (correto).

## Conteúdo (5 commits de limpeza consolidados)

### Changed
- Repo/tooling: consolidação da `knowledge-base/` da raiz → `.claude/knowledge-base/` (100 artefatos únicos via `git mv`, zero conflito; references dupe descartadas; `statusline.sh` both-layout). Um único knowledge-base.
- Benchmarks (harness interno): review de limpeza — método morto `pg_duckdb_available` removido do core `theodb_bench/db.py`, `run_m59_aq.py` default `--runs` 1→3, `ruff --fix` F401/F541, 6 harness órfãos arquivados.

### Fixed
- Build: `Makefile` `PARTS` sem `sql/85-theodb-htap.sql` (drift vs Dockerfile) — install base local ficava sem `theodb.htap_refresh`/`olap`. Imagem Docker shipada nunca afetada.

## Notas de processo (honestidade)

- Release de housekeeping cortado sem cycle-review formal por decisão do owner (opção "patch transparente"), justificado por ser 100% não-produto.
- Versão ancorada em `v0.131.0` (a tag real) — o `git describe` do `develop` retornava `v0.70.0` (defasado; tags de release vivem nos merges de `main`, fora da linha do `develop`).
- Pendência conhecida (não bloqueante, do review de benchmarks): `run_m30`/`run_m61` + testes medem `pg_duckdb`/`pg_mooncake` removidos — arquivar ou reescrever own-code num próximo passo.
