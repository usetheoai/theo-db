# Release v0.131.2

**Date:** 2026-07-22
**Verdict:** RELEASED
**Tipo:** housekeeping (patch) — **sem mudança de produto** (`theodb_rs/`/`sql/` diff vazio); só `docs/features/`
**Source review:** `/review` de `docs/features/` com 3 revisores especialistas (vetorial/AI-in-SQL/copy) + verificação independente contra o código. 1 BLOCKER + HIGHs (SQL que quebrava se copiado) + drift do pgvector (removido no M70) corrigidos; doc 04 (redundante) removido. Sem report cycle-review formal em `knowledge-base/reviews/` (o do M143 já foi consumido pela v0.131.0).
**PR:** https://github.com/usetheodev/theo-db/pull/162
**Merge commit:** 5970afeffc0dc6978fe9a3d930bd891dd7988c06
**Tag:** v0.131.2 (anotada)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.131.2
**ROADMAP checkbox:** N/A — plan sem `milestone_id` (housekeeping off-roadmap); flip pulado com WARN (correto).

## Conteúdo

### Changed
- Docs (`docs/features/`): revisão completa de exatidão vs código (3 revisores + verificação). Corrige exemplos de SQL que quebravam se copiados — assinaturas reais de `ai.nl_*` (doc 12), reloptions do `theodb_hnsw` (removido `WITH (m/ef_construction)` inexistentes, doc 02), `pq_bits=4`/`separate_storage=1` (doc 05), `theodb.embed(content,model)` no lugar de `embedding()` + `lexical_engine=ts_rank_cd` (doc 06), `ai.if` (doc 07); e o drift do pgvector/pgvectorscale (removidos no M70): 13× `CREATE EXTENSION vector`→`theodb CASCADE` + seções "coexiste/instala pgvector" → own-code `theodb_hnsw`/`theodb_ivfflat`. Doc 04 (indice-ivf) removido (redundante com o doc 03).

## Notas de processo (honestidade)

- Terceira release de housekeeping consecutiva cortada sem cycle-review formal por decisão do owner (política "patch transparente" estabelecida na v0.131.1), justificada por ser não-produto (documentação).
- Diferente das anteriores, esta teve uma revisão adversarial real (3 agentes) dos docs, só sem o report formal.
- Versão ancorada em `v0.131.1` (a tag real) — o `git describe` do `develop` retorna `v0.70.0` (defasado; tags de release vivem nos merges de `main`).
