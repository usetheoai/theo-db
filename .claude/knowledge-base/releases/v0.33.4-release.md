# Release v0.33.4

**Date:** 2026-07-03
**Verdict:** RELEASED
**Bump:** patch (v0.33.3 → v0.33.4; internal build-time optimization, no new API, no breaking)
**Source review:** .claude/knowledge-base/reviews/m43-hnsw-build-review-2026-07-03.md (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/39
**Merge commit:** addf72bdb14eb8655849038f1d54a5bb843e4b66
**Tag:** v0.33.4 (annotated)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.33.4
**Roadmap flip:** M43 already [x] — no-op.

## Shipped

- **M43** — theodb_hnsw build-time optimization (Rust product code): the in-memory graph
  build uses the SIMD AVX2+FMA kernel (l2_distance_simd, reusing the M31b kernel via an
  f32->bytes reinterpret with a #[cfg(target_endian="little")] guard + BE scalar fallback)
  instead of scalar L2. A/B: build 2.20× @200k (200±23s -> 91±3s, 3 samples, std bands
  separated) / 24min -> 8.4min @1M, recall PARITY (identical @200k). l2_distance
  (pgvector-parity) untouched. rust-pgrx safety audit SOUND. 8/8 test_index_am.py green.
  docs/benchmarks/m43-hnsw-build.md.

## Context

Completes the vector-carrier arc M41 (scan 1.2-1.5×) -> M42 (SIFT1M real-data superiority
signal) -> M43 (build 2.2×): theodb_hnsw is now competitive on build, scan, AND recall×QPS.
