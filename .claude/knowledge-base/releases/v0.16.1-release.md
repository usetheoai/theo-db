# Release v0.16.1

**Date:** 2026-06-29
**Verdict:** RELEASED
**Source review:** .claude/knowledge-base/reviews/faang-restructure-slice-1-review-2026-06-29.md (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/18 (MERGED 2026-06-29T12:04:17Z)
**Merge commit:** c391db35cbadd8c2c89ea6f886ebf6dc12c576f6
**Tag:** v0.16.1 (annotated, on the merge commit)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.16.1
**Milestone:** none (off-roadmap structural refactor — checkbox flip skipped with WARN, per cycle-release)

## Release notes

**FAANG restructure — Slice 1 (pure refactor, parity proven):** first slice of the
`v2-system-design-and-repo-structure` blueprint (SHIPPABLE 97.8).

- Root scripts moved to `scripts/` (`git mv`, history preserved); live refs repointed (CI + migration guide).
- `theodb_rs/src/lib.rs` split into the blueprint's 3-boundary layering: `pg.rs` (Postgres/pgrx glue:
  typed errors + GUC), `embed.rs` (domain: the HTTP call + parse), `lib.rs` (api-surface: `#[pg_extern]` +
  the `theodb.embed` SQL wrapper).
- Rust toolchain pinned (`theodb_rs/rust-toolchain.toml` = 1.91.0).
- **No behavior change:** identical SQL surface (`theodb.embed` / `theodb_rs._embed_text`), the 18-test suite
  green UNCHANGED, `cargo clippy` 0 warnings, and a pre-vs-post-split interleaved parity benchmark with the
  delta within 1σ (`docs/benchmarks/faang-restructure-slice-1-parity.md`).
- Cargo workspace + new CI deferred (blueprint ADR-2 — YAGNI; only 1 crate today).

## Deferred (honest)

- GHCR image publish remains a manual step (token lacks `write:packages`) — does not affect the tag/release.
