# Release v0.19.0

**Date:** 2026-06-30
**Verdict:** RELEASED
**Milestone:** M19 (ROADMAP-v2) — NL→SQL + híbrida + import próprios em Rust (fim do plpython3u)
**Source review:** `.claude/knowledge-base/reviews/m19-nl-hybrid-import-rust-review-2026-06-30.md` (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/21
**Merge commit:** fa9de4c2a98a7fbd902c139ef0f158b3bb3cb8ad
**Tag:** v0.19.0 (annotated, → merge commit)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.19.0
**Bump:** minor (v0.18.0 → v0.19.0) — backward-compatible internal port (AMBIGUOUS auto-derivation; human chose minor)

## Release notes

### Changed
- **(M19) NL→SQL, busca híbrida e import Pinecone portados para Rust/pgrx — `theodb` 100% Rust, fim do `plpython3u`:**
  - `ai.nl_to_sql` (a ÚLTIMA `plpython3u`) → Rust (`theodb_rs/src/nl.rs`); L1/L2/L4 anti-injection byte-fiel; L4 EXPLAIN-over-SPI lê `pgrx::Json`; query não-planejável → 22023 via `PgTryBuilder`. `ai.nl_query` keeper plpgsql L3 (ADR D6/F).
  - `ai.hybrid_search_rrf` + `ai.hybrid_search(jsonb)` → Rust (`hybrid.rs`); entrypoint Rust orquestra a fusão RRF via SPI (`%I` nativo). Co-residem com `theodb.embed` em `theodb_rs` (ADR D7, AskUserQuestion).
  - `theodb.import_pinecone` (FUNCTION) → Rust (`migrate.rs`); chunked PROCEDURE fica plpgsql (ADR D4).
  - **Validado:** 168 passed / 7 skipped; `cargo clippy -D warnings` limpo; benchmark nl Rust-vs-plpython3u no-regression (~0.88×); extensão 100% `plpython3u`-free (`requires = 'vector, vectorscale'`, `default_version` 1.3, migração 1.2→1.3).

## Post-merge actions

- Annotated tag `v0.19.0` pushed → merge commit `fa9de4c`.
- GitHub release published.
- ROADMAP-v2.md: `### M19 — [ ]` → `[x]` (single-flip invariant honored).
- Roadmap run-file: `.claude/knowledge-base/roadmap-runs/M19-2026-06-30.md` (status: completed).
