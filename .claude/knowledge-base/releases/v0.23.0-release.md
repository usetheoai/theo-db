# Release v0.23.0

**Date:** 2026-06-30
**Verdict:** RELEASED
**Milestone:** M23 — Control plane em Go (operador K8s + CLI)
**Source review:** `.claude/knowledge-base/reviews/m23-control-plane-go-review-2026-06-30.md` (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/25
**Merge commit:** 9cdfa9a79b6fe7ec8cf2bace991c17047576177e
**Tag:** v0.23.0 (annotated)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.23.0

## Summary

M23 ships TheoDB's own Go Kubernetes control plane: a new `operator/` module
(kubebuilder/controller-runtime) that reconciles a `TheoDBCluster` CRD into a StatefulSet of N
theo-db instances + a governing headless Service (stable pod DNS) + a gateway ClusterIP Service,
owner-referenced, with status. Plus a `theodbctl` cobra CLI (apply/get/delete) and a reproducible
`config/` kustomize deploy with least-privilege RBAC + a non-root distroless manager image.

Milestone evidence is a **real-`envtest` reconcile gate** (in-process kube-apiserver+etcd) — 14
tests, 6 real-envtest reconcile gates, 69.4% controller coverage — plus a real-kind deploy + CLI
smoke (`docs/benchmarks/m23-operator-reconcile.md`). Boundary validation (image required, storageSize
quantity pattern + immutable via CEL, port 1..65535, instances ≥ 1) rejects malformed CRs at the API
server. Std `testing` only (no ginkgo); deps Apache-2.0/BSD (no AGPL — D1 satisfied).

## Review trail

- 6-agent `/review` → 5/6 NEEDS_FIXES (12 HIGH/MEDIUM findings incl. MustParse panic/DoS + missing
  headless Service).
- Batch fixed (commits `7661e6f`, `a33edfd`) → 5-agent re-review → all READY_TO_MERGE.

## Deferred to M24

- Converge StatefulSet container port on a `spec.Port` change (cosmetic LOW).
- HA-failover orchestration + HTTP/pooler gateway (explicit M23 out-of-scope, ADR D3/D4).
