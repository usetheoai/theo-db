# Release v0.13.0

**Date:** 2026-06-28
**Verdict:** RELEASED
**Milestone:** M14 — ScaNN-quality / fork-trigger evaluation (spec 05) → decision NO-FORK (provisional)
**Source review:** `.claude/knowledge-base/reviews/m14-scann-fork-eval-review-2026-06-28.md` (READY_TO_MERGE)
**Plan:** `.claude/knowledge-base/plans/m14-scann-fork-eval-plan.md` (`milestone_id: M14`)
**PR:** https://github.com/usetheodev/theo-db/pull/14
**Merge commit:** d6f2fa159bd1fc1bf739fcce36250cb89ef30763
**Tag:** v0.13.0 (annotated, on the merge commit)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.13.0

## Scope

M14 closes feature 05 as a measurement + decision milestone (PRD fork-gate policy / measurement-first):
a reproducible benchmark (`benchmarks/scann_fork_eval.sh`, runs=3, seed=14) shows DiskANN (StreamingDiskANN,
pgvectorscale) crosses the ScaNN-quality recall bar (recall@10 0.934 @ sls=500, 0.978 @ sls=1000 ≥ 0.90), and
ADR 0004 records a **NO-FORK (provisional)** decision with two evidence-gated re-open paths (recall-rescue on
a real dataset + the LOCKED ADR 0002 superiority bet). No native `theodb_scann` AM is built (anti-sunk-cost);
DiskANN is the delivered permissive ScaNN-quality equivalent (`USING diskann`). Honest: no "ScaNN delivered"
claim; synthetic dim=32 + recall-only scope disclosed; no new dependency; harness unchanged.

## Gates

- plan-confidence SHIPPABLE 100.0 · code-quality PASS · review READY_TO_MERGE (after live-verified fixes:
  ADR-0002 consistency, provisional status, runs=3, no-clobber artifacts, self-sufficient diskann test).
- ScaNN-quality bar test + diskann/hnsw/ivfflat green; overclaim grep = 0; harness code unchanged.

## Milestone significance

With M14, **all 14 docs/features gaps captured by `/roadmap-feature` (M9–M14) are released** — the
`docs/features/` catalog (specs 01–12) now has a functional, evidence-backed surface end to end (spec 05
closed honestly as NO-FORK / DiskANN). Remaining roadmap milestones are strategic only: M5 (K8s deploy) + M8
(scale/observability/DX).

## Commits (develop → main)

- `5422460` chore(plan) · `f7ce221` feat(m14) · `064ec37` fix(m14) · `c25f54b` docs(m14) · `798fcb0` chore(release): 0.13.0
