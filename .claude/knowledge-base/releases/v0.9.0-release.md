# Release v0.9.0

**Date:** 2026-06-28
**Verdict:** RELEASED
**Milestone:** M9 — Índice IVFFlat / IVF validado + benchmarkado (features 03/04)
**Source review:** `.claude/knowledge-base/reviews/m6f1-ivfflat-index-review-2026-06-28.md` (READY_TO_MERGE)
**Plan:** `.claude/knowledge-base/plans/m6f1-ivfflat-index-plan.md` (plan-confidence SHIPPABLE 98.0; `milestone_id: M9`)
**PR:** https://github.com/usetheodev/theo-db/pull/10
**Merge commit:** 1fce11273cfe978bb179841041cc475125bae5be
**Tag:** v0.9.0 (annotated, on the merge commit)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.9.0

## Scope

M9 closes `docs/features/03-indice-ivfflat.md` and `04-indice-ivf.md`: the pgvector IVFFlat index is now a
first-class index in the recall@k harness (`--index ivfflat` / `--index all`), validated by an integration
test (recall@10 measured, index actually used, monotone across the probes sweep) and a measured
report `docs/benchmarks/m9-ivfflat.md` (IVFFlat vs HNSW recall × QPS, build-time, index size). Feature 04
(generic "IVF") is closed by the same validation — pgvector's IVF index IS IVFFlat.

Also in this release: the `/roadmap-feature` audit appended M9–M14 (the remaining `docs/features` gaps) to
ROADMAP.md as auditable milestones.

## Commits (develop → main)

- `bbe933c` chore(roadmap): add M9–M14 feature-gap milestones + M9 IVFFlat plan (/roadmap-feature)
- `8b73643` feat(m9): validate + benchmark IVFFlat/IVF in recall harness (T1.1, T2.1)
- `936666b` fix(m9): review fixes — clamp ivfflat probes before dedup; honest report; unit tests
- `f7ab246` docs(m9): code-quality audit (PASS) + review report (READY_TO_MERGE)
- `b91364b` chore(release): 0.9.0

## Gates (honest)

- plan-confidence: SHIPPABLE 98.0 · code-quality: PASS 100 · review: READY_TO_MERGE (1 HIGH + MEDIUM/LOW fixed + re-verified live)
- 72 unit + 4 index integration tests green; ruff + vulture clean; no new dependency (Rule 9); full backward compat.
- No unbenchmarked perf claim; build-time reported as load-dependent (no fixed multiplier); trade-off measured.

## Note — version computation

`compute_next_version.py` relies on `git describe --tags`, which fails in this develop→main flow (tags live
on the main merge commits, outside develop's ancestry) and fell back to v0.0.0. The next version was computed
from the highest existing tag (v0.8.0) → v0.9.0. Follow-up: harden the script to use `git tag | sort -V | tail -1`.
