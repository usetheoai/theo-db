# Release v0.37.0

**Date:** 2026-07-05
**Verdict:** PR_OPEN_AWAITING_APPROVAL
**Source review:** `.claude/knowledge-base/reviews/m46-hnsw-highrecall-qps-review-2026-07-04.md` (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/45
**Merge commit:** (pending human approval)
**Tag:** v0.37.0 (pending — cut on the merge commit post-merge)
**GitHub release:** (pending)
**Milestone:** M46 (`knowledge-base/plans/m46-hnsw-highrecall-qps-plan.md`, `milestone_id: M46`) — checkbox flip pending post-merge

## Bump derivation

`v0.36.0` → `v0.37.0` (minor): `[Unreleased] § Added` non-empty (M46 driver), no `Removed`/`BREAKING` trigger.

## Resume (post-merge)

After the PR is merged on GitHub, re-invoke `/release --resume 45` to run Step 7 (annotated tag on the merge
commit + GitHub release) and Step 7.5 (flip ROADMAP.md `## M46 — [ ]` → `[x]` + roadmap-runs record).

Note: the git remote uses the SSH alias `github-usetheo`, so `gh` needs `--repo usetheodev/theo-db` explicitly.
The last release tag is not an ancestor of `develop` (develop→main model), so version detection reads the latest
global tag (`git tag -l 'v*' | sort -V | tail -1`), not `git describe`.

## Release notes

M46 — theodb_hnsw scan hot-path hygiene (recall-neutral). `traverse` pre-sizes the three per-query structures
and reuses one neighbor scratch instead of allocating per node (zero new dependency). Recall-neutral PROVEN
(index-scan == exact seqscan, byte-identical). Hardened high-recall benchmark harness added. QPS win NOT claimed —
honest-negative under dev-box contention (pgvector control drifted +122%); the clean win/variance verdict is
deferred to a same-graph measurement on a quiet box (FU-1). Review: READY_TO_MERGE (2 HIGH resolved).
