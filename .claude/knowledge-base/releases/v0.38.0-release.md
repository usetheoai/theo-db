# Release v0.38.0

**Date:** 2026-07-06
**Verdict:** RELEASED
**Source review:** `.claude/knowledge-base/reviews/m48-am-crash-safety-review-2026-07-06.md` (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/48
**Merge commit:** 055a58107acc9b370ec2a7a72eb3ae7f7561dd7f
**Tag:** v0.38.0 (annotated, points at merge commit 055a581)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.38.0

## Bump derivation

- Last tag: `v0.37.0`.
- `[Unreleased]` had non-empty `### Removed` (postgresql-plpython3 dropped from the image) → but the project is
  in the 0.x range, so `compute_next_version.py --bump auto` yields a MINOR bump (0.x-aware: breaking → minor,
  never a premature jump to 1.0.0 which would imply a production-ready claim without sustained evidence).
- **Next version: v0.38.0.**

## Scope

This release cuts the full `[Unreleased]` accumulated since v0.37.0:
- **M48 — Index AM crash-safety & durability** (the headline): #46 (INIT-fork WAL) closed; #47 (VACUUM
  non-atomic rewrite) silent corruption eliminated via meta-pivot; pending-fold threshold; cancellable
  build+VACUUM; honest amcostestimate; maintenance benchmark artifact. Full `/review` = READY_TO_MERGE (4
  domain reviewers). 76 tests passed on the RC image.
- Prior unreleased work: postgresql-plpython3 removed from the image (dead weight since M19); roadmap
  amendment M47–M55; FU-1 same-graph scan micro-bench.

## Post-merge steps (on `/release --resume` after the human merges PR #48)

1. Cut annotated tag `v0.38.0` pointing at the merge commit; push it.
2. `gh release create v0.38.0` with the rendered notes.
3. ✅ DONE — ROADMAP.md M48 flipped [ ]→[x] on develop (commit 498c29a); roadmap-run M48-2026-07-06.md written.

## Release notes

(rendered from CHANGELOG [0.38.0] — see PR #48 body and `CHANGELOG.md`.)
