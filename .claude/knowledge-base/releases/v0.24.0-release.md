# Release v0.24.0

**Date:** 2026-07-01
**Verdict:** RELEASED
**Milestone:** M24 — Observabilidade + escala em Go (read pools, OTel/Prometheus, MCP)
**Source review:** `.claude/knowledge-base/reviews/m24-observability-readpool-mcp-review-2026-07-01.md` (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/26
**Merge commit:** 40bc5e0cebcbe350441928986f5a20035ba2d502
**Tag:** v0.24.0 (annotated)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.24.0

## Summary

M24 layers three own-Go capabilities onto the M23 operator: (1) **domain Prometheus metrics**
(reconcile counter/histogram + per-cluster phase/ready/desired gauges) on controller-runtime's shared
registry — zero new dependency, bounded cardinality, series cleaned on cluster deletion; (2) a
**read-routing Service** `<name>-ro` (read-scale endpoint; streaming-replication routing is M25); (3) a
read-only **MCP server** (`cmd/theodb-mcp`, official Go SDK v1.6.1) exposing `list_clusters` +
`get_cluster` to AI agents over stdio (default) / streamable HTTP.

Evidence: real-`envtest` metric + read-Service gates, in-memory MCP handshake/tool-call/failure-scenario
tests, MCP tool-call benchmark (~208 µs/op). `govulncheck ./...` reports 0 reachable vulnerabilities
(toolchain go1.25.11; x/net v0.55.0; otel/sdk v1.40.0). Coverage: metrics 90.9%, mcpserver 82.1%,
controller 71.9%. `docs/benchmarks/m24-observability-readpool-mcp.md`.

## Review trail

- 6-agent `/review` → 3 NEEDS_FIXES (metric series-leak HIGH, phase-metric gap, untested failure
  scenarios, `-http` unauth undocumented, raw-error SA leak).
- Batch fixed (`31a7b7d`, `f7df488`) → 3-agent re-review → all READY_TO_MERGE.

## Roadmap

M24 is the **last milestone of ROADMAP-v2** (M17–M24). With M24 `[x]`, the V2 vector-pillar +
own-database + Go control-plane scope is fully delivered.

## Deferred to M25+

- Read-pool real read-scale (streaming replication + replica-only routing / PgBouncer).
- MCP write tools (apply/delete) — need the auth story extended first.
