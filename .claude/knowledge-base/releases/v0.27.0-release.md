# Release v0.27.0

**Date:** 2026-07-01
**Verdict:** RELEASED
**Milestone:** M31 (index AM latency — structured partial-page reads)
**Source review:** .claude/knowledge-base/reviews/m31-am-latency-review-2026-07-01.md (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theo-db/pull/29
**Merge commit:** ca979bf394943b7277ae7e79204c02960b66c2c8
**Tag:** v0.27.0 (annotated)
**GitHub release:** https://github.com/usetheodev/theo-db/releases/tag/v0.27.0

## Release notes

### Added
- M31b milestone (ADR 0011): SIMD vector distance (AVX2 + runtime dispatch) to close the residual latency gap vs
  pgvector. Sequenced M31 → M31b → M32 in the P0 vector-superiority track.

### Changed
- M31 — `theodb_ivfflat` structured partial-page reads: the Index Scan reads only the probed lists' pages
  (O(probes)), not the whole blob (O(N)). Measured ~45× faster than the M26 O(N)-per-scan path (~38 ms vs
  ~1700 ms at 100k×128), recall preserved, INSERT/DELETE/VACUUM intact. Honest: still ~2.7× behind pgvector's
  AVX-SIMD C — the algorithmic gap is closed; the constant-factor (SIMD) residual is M31b (ADR 0011). Evidence:
  `docs/benchmarks/m31-am-latency.{md,json}`.

## Evidence

- 2-agent /review: 1 BLOCKER (empty-built-index pending drop) found + fixed + regression-tested; FFI safety +
  re-scope honesty confirmed SOUND_AND_HONEST.
- 51 tests green (index-AM incl. empty-built + 100k latency + M20–M22 coexistence); clippy -D warnings clean.
