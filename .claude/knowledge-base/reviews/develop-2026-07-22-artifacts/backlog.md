# Review backlog — develop (diff)

- Iteration: 0
- Verdict: IN_PROGRESS
- Files fully reviewed: 9 / 9
- Open findings: 30 (3 blocking, 27 advisory)

## Pillar status (derived)

| Pillar | Status | Files done | Open findings | Blockers |
|---|---|---|---|---|
| code | COMPLETED | 9/9 | 4 | 1 |
| architecture | COMPLETED | 9/9 | 2 | 0 |
| system_design | COMPLETED | 9/9 | 3 | 0 |
| design_patterns | COMPLETED | 9/9 | 2 | 0 |
| semantic_folders | COMPLETED | 9/9 | 2 | 0 |
| security | COMPLETED | 9/9 | 1 | 0 |
| tests_deadcode_docs | COMPLETED | 9/9 | 2 | 0 |
| license_provenance | COMPLETED | 9/9 | 4 | 0 |
| idiomaticity | COMPLETED | 9/9 | 3 | 0 |
| maintainability_extensibility | COMPLETED | 9/9 | 7 | 0 |

## Open blocking findings (must fix)

- [BLOCKER] code docs/features/13-grafo-nativo.md:75 — Doc-code contract violation: the example errors when copied verbatim. `theodb.graph_expand` RETURNS SETOF bigint (theodb_rs/src/graph.rs:557), so in FROM its default output column is named `graph_expand`, not `node` — `SELECT node FROM theodb.graph_expand(...)` fails with `ERROR: column "node" does not exist`. The extension's own SQL proves this: graph_rag.rs:85 must alias it `AS t(node_id)` to name the column.  (finding 573f6c402ab07757)
- [HIGH] system_design docs/features/14-analitico-colunar.md:18 — Missing operational caveat on documented DML surface (fail-fast contract undisclosed): the columnar TableAM supports only INSERT — UPDATE, DELETE, tuple-lock, parallel scan, bitmap scan, sample scan, TID-range scan and index build are typed-error stubs (theodb_rs/src/am/columnar.rs:15 'UPDATE/DELETE/tuple-lock/parallel/bitmap/sample are typed-`error!` stubs', :237, :250-306). The 'Caveats honestos' block lists three caveats but omits the largest one: a user following the doc's flow who then runs UPDATE/DELETE/CREATE INDEX on the table hits an error, and no doc section warns them. 'Append-only' appears only as a storage-format adjective, not as a DML contract.  (finding d2d823232022541a)
- [HIGH] system_design docs/features/14-analitico-colunar.md:81 — Data-flow claim at the TAM seam is false and contradicts the project's own recorded limit (honest-limits principle). The doc states a plain SELECT decodes only projected columns, but the plain seqscan path decodes ALL columns of every stripe (theodb_rs/src/am/columnar.rs:1015-1021 — load_next_batch builds coldesc for 0..natts and decode_stripe over the full tupdesc; projection pushdown exists only in the M100 CustomScan path via read_stripe_columns' projection arg, columnar.rs:697-709). The project's own benchmark docs/benchmarks/m99-columnar-tam.md records plain seqscan as 'at parity-or-slower than heap, by design' (~16-26x slower measured) precisely because 'a plain seqscan receives no projection'. Presenting this as 'a forma de uso padrão' without the measured slowdown misleads operators.  (finding 52f0c1037a2d1133)

## Advisory (LOW/INFO) — not blocking

- [MEDIUM] code docs/features/13-grafo-nativo.md:51 — Doc-code contract: every function documented on this page is `REVOKE ALL ... FROM PUBLIC` in the extension SQL (theodb.graph_* at graph.rs:568-577; ai.extract_entities/ai.extract_graph/theodb.graph_upsert at graph_extract.rs:374-376; theodb.graph_embed_nodes/graph_rag_search at graph_rag.rs:97-98; theodb.embed at api.rs:472), yet the page never states the privilege requirement. A non-superuser (or any role without an explicit GRANT) following the doc gets `permission denied` on every example. The sibling doc (15-lakehouse-parquet.md § 2) documents the same REVOKE pattern explicitly — this page omits it.
- [MEDIUM] code docs/features/18-motor-lexical-bm25.md:162 — Fabricated citation (doc-code contract): the cited test name `hybrid_bm25_requires_content_text_col` does not exist anywhere in the source tree. The behavior IS tested, but under the name `hybrid_bm25_without_text_col_errors` (theodb_rs/src/am/hnsw_page/tests.rs:921). A reader grepping the cited symbol finds nothing — the same defect class the project's golden rules treat as citation fabrication.
- [MEDIUM] design_patterns docs/features/14-analitico-colunar.md:105 — Pattern 'metadata-only aggregate fast-path' — verdict: misapplied-in-doc. The doc claims count(*) (kind 0) 'responde do metadado colunar sem materializar linhas', but the code has no metadata fast-path for count(*): theodb_rs/src/am/df_executor.rs:199 builds a DataFusion `count(lit(1))` expression and :245 explicitly projects column 0 because 'count(*) needs a column to establish the row count' — i.e. the scan decodes at least one column's chunks into Arrow batches and DataFusion counts the rows. The genuine directory-only (metadata) fast-path exists ONLY for min/max (kinds 6/7, directory_minmax — correctly described in the doc's section 13). Attributing the metadata-only shape to count(*) transfers the min/max pattern to an aggregate that actually runs the vectorized decode path.
- [MEDIUM] idiomaticity docs/features/14-analitico-colunar.md:147 — SQL example references column `nome_texto`, which does not exist in the `eventos` schema defined in §2 (id, ts, regiao, valor) — copied verbatim it fails with 42703 'column does not exist' instead of demonstrating the intended decline-to-native-plan behavior for min/max on an unordered type.
- [MEDIUM] license_provenance .claude/knowledge-base/releases/v0.131.2-release.md:7 — external-project reference (github.com reference) — verify license/attribution
- [MEDIUM] license_provenance .claude/knowledge-base/releases/v0.131.2-release.md:10 — external-project reference (github.com reference) — verify license/attribution
- [LOW] architecture docs/features/19-quantizacao-vetorial.md:4 — Inconsistent component status across the doc set: doc 17's banner classifies theodb_symqg as 'AM alternativo / experimental' and 'não é o default recomendado' (hnsw 2.6-3.9x faster, gate NOT met), while doc 19 presents it neutrally as one of 'os três access methods own-code theodb_ivfflat / theodb_hnsw / theodb_symqg' under a blanket '✅ Entregue' status, with only a bare link to the E2 verdict in §11. Same component, two different maturity framings — a reader landing on doc 19 first can adopt symqg without the not-recommended caveat doc 17 treats as 'honestidade crítica'.
- [LOW] architecture docs/features/19-quantizacao-vetorial.md:58 — DRY across the doc set: the same architectural fact (theodb_symqg is L2-only, opclass theodb_symqg_l2_ops) is independently restated with divergent line citations — doc 17:6-7 cites 'theodb_rs/src/am/mod.rs:359-360' while doc 19:58 cites 'mod.rs:355-359' (and 'mod.rs:338-380' for the opclass roster). Both currently resolve to the same extension_sql! block (CREATE OPERATOR CLASS at mod.rs:359), but two independent citations for one fact means the next code move breaks them asymmetrically, leaving the docs disagreeing about where the L2-only constraint lives.
- [LOW] code docs/features/18-motor-lexical-bm25.md:32 — Doc-code contract (minor): `ai.hybrid_search_rrf` is `REVOKE ALL ... FROM PUBLIC` (theodb_rs/src/api.rs:758) but the page never mentions the privilege requirement; a non-granted role following § 2 verbatim gets `permission denied` rather than results.
- [LOW] idiomaticity docs/features/14-analitico-colunar.md:211 — Prose lists `bool` among the types whose min/max is answered from the zone-map directory, but PostgreSQL has no `min(boolean)`/`max(boolean)` aggregate — `SELECT min(some_bool)` fails with 42883 'function min(boolean) does not exist', so the stated form is not invocable at SQL level (the internal MinMaxKind supporting bool in columnar_agg.rs:374-378 is unreachable via these aggregates). Non-idiomatic PostgreSQL claim.
- [LOW] maintainability_extensibility docs/features/14-analitico-colunar.md:92 — The `theodb.enable_columnar_agg` 'default OFF' fact is restated 4 times (lines 18-19, 92-94, 285, 299). If the default ever flips to ON (a plausible graduation of an opt-in), four sites must change in lockstep — duplication of a single volatile configuration fact.
- [LOW] maintainability_extensibility docs/features/14-analitico-colunar.md:213 — Perf multipliers are repeated between the Status header and body sections ('~1300–1400×' at lines 12 and 213; '7,29×' at 12 and 186; GROUP BY '4,53–9,75×' at 10-11 vs '6,00×/4,53×/9,75×' at 167-168) — each pair is a drift site vs the single benchmark artifact when re-measured.
- [LOW] maintainability_extensibility docs/features/15-lakehouse-parquet.md:6 — 12 `parquet.rs:NNN` / `85-theodb-htap.sql:NNN` line-number pins through the body (`:121`, `:168`, `:75`, `:321`, `:185`, `:227`, `:235`, `:50`, `:30`, sql `:39/:62/:93/:120/:144`) vs the 0-2 header-only convention of docs 01-12; no verified-at marker to detect rot.
- [LOW] maintainability_extensibility docs/features/16-vectorizer.md:12 — Second consecutive status blockquote duplicating the delivery claim of the first (both open with '✅ Entregue'), deviating from the single-Status-block template of docs 01-12; the dogfood/M141 caveat it carries is additionally restated in the closing note (line 341-343), so the same volatile pre-1.0 status lives in 3 places.
- [LOW] maintainability_extensibility docs/features/17-indice-symqg.md:5 — 12 file:line pins including repeated ones (`am/mod.rs:87` at lines 5 and 35; `mod.rs:359-360` and `build.rs:456-459` each twice at 6-7 and 51-52) — the same volatile line offsets duplicated within the doc, doubling the rot surface vs citing them once in the header per the 01-12 convention.
- [LOW] maintainability_extensibility docs/features/19-quantizacao-vetorial.md:238 — Section 11 re-documents `theodb_symqg` facts already owned by the sibling doc 17-indice-symqg.md — degree_bound default/range 32…512, round-up-to-multiple-of-32 behavior, L2-only opclass with the same `mod.rs:355-359`/`options.rs:73-75` pins. Cross-doc duplication of the same knowledge: a symqg change now requires synchronized edits in two feature docs (DRY at the doc level).
- [LOW] security docs/features/16-vectorizer.md:40 — The documented credential-provisioning example `ALTER SYSTEM SET theodb.embedding_api_key = '...'` (repeated in the full-flow at line 311) persists the API key in plaintext in postgresql.auto.conf and, when log_statement/pgaudit is enabled, records the literal key in the server log and in the operator's client history (CWE-312 cleartext storage / CWE-532 insertion of sensitive information into log file). The mechanism itself matches the project's sanctioned posture (GucContext::Suset, value hidden from non-superusers, dead-letter redaction — all correctly documented in this same file), but the doc gives no caveat that the SET statement/auto.conf can expose the key outside that GUC protection. Placeholder 'sk-...' is not a real secret (verified — no live credential in the diff).
- [LOW] semantic_folders docs/features/13-grafo-nativo.md:1 — Findability: docs/features/ now holds 19 numbered capability docs but has no README.md/index.md listing them; navigation depends entirely on filename scanning. The only inbound index-like reference (docs/quickstart.md:3) points at the folder generically without enumerating entries, so nothing needed updating for the 7 new docs — but the absence of an index means growth (12 -> 19 files in this change) keeps raising the discovery cost.
- [LOW] system_design docs/features/16-vectorizer.md:63 — Worker-lifecycle caveat is stated but its failure mode is not: the honesty note says the single worker is fixed to database 'postgres' (verified: WORKER_DBNAME = "postgres", theodb_rs/src/vectorizer.rs:662), yet the doc never spells out the consequence — a vectorizer declared in any other database enqueues jobs that are NEVER drained (silent pending accumulation), the exact failure shape the doc does warn about for the missing shared_preload_libraries case ('a fila acumula jobs pending mas nada os drena'). Symmetric disclosure of the same backpressure failure mode is missing for the wrong-database case.
- [INFO] design_patterns docs/features/16-vectorizer.md:166 — Pattern 'queue done-job disposal' — verdict: applied_correctly with an imprecise analogy. The shape claim matches the code (done jobs are DELETEd; theodb_rs/src/vectorizer.rs dead-letter purge comment: '`done` jobs are already DELETEd; this bounds the retained `failed` history'). However the analogy 'à la `pgmq.archive`' names the wrong pgmq operation: pgmq.archive RETAINS the message in an archive table, while pgmq.delete removes it — the TheoDB behavior corresponds to pgmq.delete. Advisory only; the described code shape itself is correct.
- [INFO] idiomaticity docs/features/16-vectorizer.md:107 — Fenced block tagged ```sql contains a bare, non-executable signature reference (`theodb.create_vectorizer(... ) RETURNS int` with no SELECT/CREATE) — copied verbatim it is a syntax error. The same file's other signature-reference block (§10, lines 179-180) follows the corpus idiom of commenting such lines with `--`.
- [INFO] license_provenance docs/features/17-indice-symqg.md:4 — Positive confirmation (no defect): the doc makes an explicit clean-room provenance claim for theodb_symqg — implemented from the paper arXiv:2411.12229 with the C++ NTUITIVE reference declared 'estudo-apenas, nunca copiada' and D1 cited. This matches the project's recorded study-only posture for the SymphonyQG reference implementation and is the correct D1-compliant framing; recorded here so the jury sees the claim was checked, not skipped.
- [INFO] license_provenance docs/features/18-motor-lexical-bm25.md:128 — Section 8 directs users to install the external extension pg_textsearch without stating its license posture. Verified in-repo: docs/adr/0003-permissive-bm25-pg-textsearch.md records it as PostgreSQL License (permissive), so D1 is NOT violated and the extension is correctly described as external/not-shipped. Advisory only: the doc is otherwise diligent about licenses (Tantivy 'MIT' at line 4), so citing the permissive posture (or ADR-0003) here would keep the license disclosure consistent and preempt reader doubt about a non-shipped dependency.
- [INFO] maintainability_extensibility docs/features/16-vectorizer.md:14 — User-facing feature doc links internal governance file paths (`.claude/rules/dogfood-golden-rule.md § 1`, `.claude/rules/public-copy.md § 3`; same pattern in 15-lakehouse-parquet.md line 13 and 19-quantizacao-vetorial.md line 332 with a `../../.claude/...` relative path). These couple public docs to the internal tooling layout, which is reorganized more freely than docs/ — a rot vector the 01-12 set mostly avoids (doc 01 cites only 'CLAUDE.md, regra TheoDB 5').
- [INFO] semantic_folders docs/features/13-grafo-nativo.md:1 — Numbering-scheme coherence: the new docs correctly continue the existing sequence at 13-19 with no duplicates. The gap at 04 (03 -> 05) is pre-existing on origin/main, not introduced by this change; noting it so the missing slot is not later mistaken for an accidental deletion of a new doc.
- [INFO] tests_deadcode_docs docs/features/17-indice-symqg.md:97 — Off-by-two line citation: the doc cites `theodb_rs/src/am/guc.rs:24` for 'default 64', but guc.rs:24 is `const MAX_EF_SEARCH: i32 = 1000;`; the default constant `DEFAULT_EF_SEARCH: i32 = 64` is at guc.rs:22. The claim itself (default 64) is correct.
- [INFO] tests_deadcode_docs docs/features/19-quantizacao-vetorial.md:78 — Minor formula drift vs the cited artifact: the doc states SBQ memory as `ceil(dim·1/8)` bytes/vector, while the cited m22-sbq-parity.md gives the word-aligned formula `ceil(dim·bits/64)·8` bytes (identical for dims that are multiples of 64 — e.g. 128d SIFT — but diverges for others, e.g. dim=100: 13 vs 16 bytes). The parity claim itself is backed by the artifact.
