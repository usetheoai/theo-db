# Review — M162 100M gap measurement (2026-07-27)

**Slug:** m162-100m-gap · **Milestone:** M162 · **Type:** measurement milestone (NO production code — benchmark harness + docs only).
**Reviewer:** council-benchmark (measurement-honesty audit of the verdict + artifacts).

**Verdict:** READY_TO_MERGE

The reviewer confirmed the underlying data is genuine (100M real, M159 cache-reuse false-green caught, failures elevated over the ratio, failed queries reported as `>`-bounds not fabricated, encoding-deferral sound per Rule 5). It flagged the **write-up framing** (not the data) as overstating the negative on three axes. All findings were corrected in the verdict + CHANGELOG before merge; the milestone conclusion (scale-limit failure is the verdict; encoding → M163) is unchanged and was explicitly credited as sound.

## Findings + resolution

| # | Sev | Finding | Fix |
|---|---|---|---|
| B1 | BLOCKER | "I/O/decode-bound, decisively" asserted without the `shared_blks_read` oracle the plan named (never captured); also incoherent with "catastrophes are materialization (CPU/alloc)" | Rewrote § I/O to "NOT ISOLATED — deciding counter not captured"; the strongest evidence (materialization catastrophes) points at materialization/decode, not a decode-byte problem an encoding fixes. Encoding-deferral re-grounded on the materialization+bug levers (independent of the I/O question). |
| H1 | HIGH | "conservative → true gap wider" is inverted; both asymmetries (CH server-side + min-of-3 vs TheoDB round-trip + median-of-3) favor CH → ratio overstates → true gap NARROWER. Contradicted the plan. | Caveat corrected: "measured ratio OVERSTATES → true gap ≤ measured (narrower)"; both asymmetries disclosed. |
| H2 | HIGH | "cold ≫ hot across the board" false (5 queries hot ≥ cold); the cited q16 60→63 example shows hot slower | Reframed as bimodal (small queries cache cold≫hot; larger-than-RAM queries cold≈hot / noisy hot>cold); q16/q18 moved to the cold≈hot group; q0/q5 as the cold≫hot examples. |
| M1 | MED | 24.3× carried by 2 outliers (q0 1495× count, q19 837× SELECT*); drop → ~15.5×; q0 is a missing-count-fast-path not a decode gap | Headline leads with per-class (8–26× / 16×) as the typical; 24.3× labeled outlier-sensitive + ~15.5× without outliers. |
| M2 | MED | 19.4× (43) vs 24.3× (19) is cross-population | Disclosed in headline + caveats; matched claim is the per-class one. |
| M3 | MED | practical completion is 19/43 (44%), not "5 failures" | Headline: "19/43 completed"; the scale-limit failure elevated as THE verdict. |
| L1 | LOW | count 99,997,497 is prose-only | Caveat cites `hits_heap reltuples = 99,955,952` + the full-stream materialization as the load evidence. |
| L2 | LOW | median-vs-min asymmetry absent from caveats | Added to caveats. |

## What was credited as honest (unchanged)

100M is real (canonical full ClickBench count); M159 1M cache-reuse false-green self-caught + disclosed; the 5 hard failures elevated above the ratio; survivorship direction honest ("similar-or-worse, never better"); encoding honest-negative (Rule 5) is a sound measurement-first call — the two top levers (q20 i32-offset bug, q0/q19 materialization) are demonstrably NOT fixable by a decode-byte encoding, so the M163 deferral holds independent of the un-isolated I/O question.

## Hard gates (cycle-review)

- Failing tests / new secrets / direct main commit / Co-Authored-By: none. CHANGELOG updated: yes. No production code changed (harness + docs only).

## Downstream

Proceed to `/release` v0.153.0 + flip M162 checkbox → ROADMAP_COMPLETE.
