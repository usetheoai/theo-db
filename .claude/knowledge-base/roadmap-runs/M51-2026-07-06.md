---
milestone_id: M51
slug: sbq-inline-am
date: 2026-07-06
status: in_progress
---
# Milestone M51 — SBQ inline no AM (quantização no caminho quente)
## Objective (from ROADMAP.md)
Códigos SBQ compactos inline no layout v3 do theodb_hnsw; scoring barato (Hamming popcount SIMD) no hot path; rerank exato f32 só no top. GATED por M50 (veredito AUTORIZOU + re-escopou). Decisão do usuário 2026-07-06: implementar + evidenciar correctness/recall≥0.99 (rerank f32) na escala VIÁVEL (25k, régua M50); o claim ≥2× QPS em escala com pressão de memória fica como follow-up EXPLÍCITO rastreado (box contendida não roda 1M×3-builds — provado no M50).
## Outcome (em progresso)
- **Plano SHIPPABLE (98.4)** committado (`d8d94a6`) — 4 fases, 9 DoD, 4 ADRs, recall-gate ≥0.99, follow-up ≥2× rastreado.
- **Harness TDD `cargo pgrx test` operacional** (`351022f`): `packaging/Dockerfile.m51-test` (builder + pgvector source + theodb umbrella + vectorscale da runtime) + `scripts/pgrx-test-in-builder.sh` (non-root uid 1001, dirs de extensão graváveis). Destrava TDD rust rápido (~2min/iter) para M51–M55 sem sudo. Fix colateral: `MemNode` deriva `Debug` (pg_test build estava quebrado em develop).
- **T1.1 (serialização do codebook) via TDD** (`916f77d`): `SbqQuantizer::to_meta_bytes`/`from_meta_bytes` (RED confirmado → GREEN, 2 pg_test passando: roundtrip byte-exato + rejeição de truncamento tipada Rule 8).

### T1.1 — feito e testado (formato)
- **Serialização do codebook** (`916f77d`): `SbqQuantizer::to_meta_bytes`/`from_meta_bytes` (2 pg_test: roundtrip byte-exato + rejeição truncamento Rule 8).
- **Meta layout v2 SBQ-capable** (`6a6147b`): `HnswMeta.sbq_bits`/`codebook`; `encode_meta` emite v1 byte-idêntico quando sbq_bits==0, v2 (`version=2 + [sbq_bits][cb_len][codebook]`) quando >0; `decode_meta` ramifica por versão → **v1 legível** (REINDEX faz upgrade). 3 pg_test: v2 roundtrip + v1 permanece 45 bytes + rejeição truncamento. **0 regressão** confirmada.
- **Débito pré-existente registrado** (`75ab8ab`, backlog): 6 `ann::hnsw::hnsw_persist_tests` não registram no pgrx (`function does not exist`) — **NÃO é do M51** (provado por worktree em 916f77d: falha idêntica); nunca rodaram porque o pg_test build estava quebrado até o fix `MemNode: Debug` (351022f).

### Build path SBQ — COMPLETO e testado (7 pg_test, byte-idêntico v1, 0 regressão)
- **T1.1 serialização** (`916f77d`): `to_meta_bytes`/`from_meta_bytes` (2 testes).
- **T1.1 meta v2** (`6a6147b`): `HnswMeta.sbq_bits`/`codebook`, v1 byte-idêntico (3 testes).
- **T2.1 element codec** (`c3243a8`): código SBQ inline após o vec, `ElementView.code_bytes` (1 teste).
- **T1.1-build + T2.1-write** (`ac0ed28`): `pack_sbq` treina quantizer + persiste codebook no meta + escreve cada código inline == quantize(vec); **fold do VACUUM preserva SBQ** (re-quantiza live vectors, sem downgrade v2→v1). 1 teste + regressão pack 3/3.
- Débito pré-existente (classe pgrx-test) registrado no backlog (não-M51, ortogonal — provado).

### WRITE side do formato SBQ — COMPLETO e testado (9 pg_test, byte-idêntico v1, 0 regressão)
- T1.1 serialização (`916f77d`), T1.1 meta v2 (`6a6147b`), T2.1 element codec (`c3243a8`), T1.1-build+T2.1-write pack_sbq+fold (`ac0ed28`), **reloption connect** (`8676aac`).
- `CREATE INDEX ... USING theodb_hnsw (v) WITH (sbq_bits=4)` produz índice v2 com códigos inline, **scannable corretamente via f32** (test `create_index_with_sbq_bits_scans_correctly`: top-5 == exato). O fold preserva SBQ.

### READ path (T3.1) — COMPLETO e testado (`5c1f1a5`)
- `traverse` num índice v2: reconstrói o quantizer do `meta.codebook`, quantiza a query 1×, pontua o walk (entry+descida+ground) por **Hamming** sobre os códigos inline, e reranqueia os sobreviventes por **f32 exato** (recall recuperado, M40). GUC `theodb_hnsw.over_fetch` alarga o pool.
- `scan_core`: `ground_search_nodes` (retorna Nodes c/ endereços p/ rerank); `ground_search` delega → **path f32 byte-idêntico** (4/4 scan_core + traverse_presize passam).
- **Gate de recall**: `sbq_traverse_hamming_then_rerank_recall_high` recall@10 ≥ 0.9 (walk_ef 300 < 400 pts, over_fetch=6) + `create_index_with_sbq_bits_scans_correctly` (ef cobre → rerank == exato).
- **12 pg_test novos do M51 no total, zero regressão.**

### Ponto de continuação — T4.1 benchmark + gate/review/release
1. **T4.1:** build imagem `theodb:m51` (Dockerfile) → benchmark recall×QPS SBQ-inline vs pgvector vs diskann na régua M50 (25k viável; ≥2× em escala com pressão de memória = follow-up rastreado, decisão do usuário) → `docs/benchmarks/m51-sbq-inline.{md,json}` + veredito D3 (recall@10≥0.99 com over_fetch tunado) + ADR keep/kill.
2. **T5.1 → review → release v0.42.0 → merge → flip M51.**

### (histórico) Ponto de continuação — READ path (T3.1) + gate
1. **T3.1 traverse (a peça mais delicada — fazer com o modelo de `traverse`+`ann/scan_core` em contexto fresco):**
   - `traverse` (`hnsw_page.rs:585`): quando `meta.sbq_bits>0`, reconstruir o quantizer de `meta.codebook`, quantizar a query 1×, e pontuar por `hamming(qcode, ev.code_bytes)` no walk (entry `:603`, descida `:610`, ground via `PageNeighborSource`/`scan_core::ground_search`).
   - Preservar os endereços `(blk,off)` no resultado para o **rerank exato f32** do top `k·over_fetch` (`load` lê `ev.vec_bytes`).
   - `over_fetch`: GUC `theodb_hnsw.over_fetch` (padrão `ef_search` em `guc.rs`), default medido.
   - RISCO: bug no walk = recall errado/crash no scan de produção → TDD com o teste de recall como gate.
2. **T4.1/T5.1:** benchmark recall@10≥0.99 na régua M50 (build imagem theodb:m51) + ADR keep/kill → code-quality → review → release v0.42.0 → merge → flip M51.

**Nada perdido:** tudo committado + pushed em develop. Harness `scripts/pgrx-test-in-builder.sh` (~2min/iter). O WRITE side pronto significa que o T3.1 só precisa do read path + rerank.
