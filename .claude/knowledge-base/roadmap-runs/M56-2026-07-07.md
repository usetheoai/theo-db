---
milestone_id: M56
slug: inplace-maintenance-tombstone
date: 2026-07-07
status: in_progress
---
# Milestone M56 — Manutenção in-place (tombstone) — remove o muro do VACUUM (ADR 0017 fase 1)
## Objective (from ROADMAP.md)
Implementa a fase 1 do ADR 0017: DELETE vira tombstone in-place por página (à la pgvectorscale/pgvector), removendo o rebuild O(N) sob EXCLUSIVE do caminho de delete. Compaction via fold M48 por threshold. Teto de memória do BUILD. Pré-requisito de v1.0 E de medir o P0 (M57) a 1M.
## DoD (6 itens)
1. DELETE tombstone in-place por página (element-tuple deleted+version), sem O(N)-RAM, sem EXCLUSIVE index-wide; reúso de slot no aminsert; scan filtra tombstones.
2. Recall preservado sob tombstones; medir recall entre compactions (decide fase 2 RepairGraph).
3. Compaction por threshold reusa fold.rs (M48), crash-safe.
4. Teto de memória do BUILD (collect_corpus streaming/batched).
5. Benchmark: parada do DELETE-path << 86s + RAM O(#deletados) -> docs/benchmarks/m56-inplace-maintenance.{md,json} vs baseline M55.
6. Layout Changed (magic bump + REINDEX); crash-injection e2e.
Deps: M55 (ADR 0017), M48 (fold.rs). Risco ALTO (imutabilidade M35 no delete path, version machine, FFI VACUUM in-place).
## Outcome (parcial — em progresso, honesto)
**Feito e testado (commitado em develop, cf9d9c2):**
- **Discover:** blueprint `m56-inplace-tombstone-blueprint.md` (council-index-storage) — achado load-bearing: o layout v2 já tem 2 pad bytes livres p/ deleted+version SEM re-empacotar (retrocompatível, REINDEX opcional). Divergência honesta: slot-reuse no aminsert é FASE 2 (quebra imutabilidade M35) → reclaim da fase 1 via compaction.
- **Fundação (DoD 1 parcial):** E_DELETED/E_VERSION nos pad bytes; ElementView.deleted/version + decode; `mark_tombstone_in_place` (in-place, idempotente, GenericXLog-ready). 3 testes puros CI verdes.

**Falta (não entregar sem isto — 100% funcional):**
- DoD 1 restante: `Cand.deleted` no scan `load`; `enumerate_entries` filtra tombstones.
- DoD 2: `ambulkdelete` sweep in-place por página (FFI, GenericXLog, sem advisory EXCLUSIVE) + pg_test.
- DoD 3: compaction reusa fold.rs + gatilho por ratio (GUC).
- DoD 4: BUILD streaming (piso O(N) do grafo declarado como limitação honesta).
- DoD 5: benchmark `m56-inplace-maintenance` — parada ≪ 86s + RAM O(#deletados); concorrência sem stall; recall entre compactions.
- DoD 6: layout Changed + crash-injection e2e (GUC de crash do M48). Depois: /review, /release, flip.

Status: `in_progress` — a fundação (o achado dos pad bytes, a parte mais incerta) está posta+testada; scan-filter + FFI ambulkdelete + compaction + benchmark + e2e restam. M57/M58/M59 não iniciados.
