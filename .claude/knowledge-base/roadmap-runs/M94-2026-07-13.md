---
milestone_id: M94
slug: per-scan-membership-scoping
date: 2026-07-13
status: completed
plan: knowledge-base/plans/per-scan-membership-scoping-plan.md
review: knowledge-base/reviews/per-scan-membership-scoping-review-2026-07-13.md
release: v0.81.0 (PR #92, merge ba3a40c)
---

# Milestone M94 — per-scan membership scoping

## Outcome

Filtered `UNION`/self-join/partitioned-`Append` vector queries now work — each vecfilter node scopes its TID
membership via a per-node thread-local registry + RAII pull-window swap-discipline (re-entrant, unwind-safe), with
xact/subxact/PREPARE cleanup. Resolves the M92/M93 review's convergent BLOCKER; the owning council re-reviewed
against the PG17 source: "genuinely fixed, not papered over". Review MEDIUMs fixed in-line (TIDBitmap-per-rescan
leak — the prior safety comment was factually wrong; unwind-safe swap). 265 tests GREEN; benchmark spot-check recall
byte-identical to v0.80.1 (QPS delta = host variance, documented). Released as v0.81.0.
