---
milestone_id: M132
slug: vectorizer-worker-embed-fix
date: 2026-07-21
status: completed
plan: .claude/knowledge-base/plans/vectorizer-worker-embed-fix-plan.md
blueprint: knowledge-base/discoveries/blueprints/vectorizer-worker-embed-blueprint.md
review: .claude/knowledge-base/reviews/vectorizer-worker-embed-fix-review-2026-07-21.md
release: v0.118.0
---

# Milestone M132 — Fix #132 (vectorizer worker embed on self-host)

## Outcome

RELEASED as v0.118.0 (PR #141, merge df841d4). HONEST-NEGATIVE on the reported symptom: #132 does NOT reproduce —
a clean end-to-end embeds 5/5 fresh rows with the queue draining to 0 failures, before and after the change. The
"NULL embedding column" was the expected shape for a chunk-mode vectorizer (chunks land in the chunk table).

Shipped instead (ADR M132-1): the diagnosability whose absence made the report cost a day — the caught SQLSTATE +
message now recorded in last_error (was a blanket literal), a worker startup line declaring the embedding-config
view (key length only), and Some(0) no longer counted as a processed batch.

Reviews: council-security + council-rust-pgrx, both 0 BLOCKER / 0 HIGH. Two MEDIUMs were defects this milestone
introduced (persisting an echoed credential; violating the H1 fencing contract by counting a lease-lost job) — both
fixed at the root and re-verified on the shipped binary. #132 CLOSED with the non-reproduction evidence. The dogfood
anchor's freshness half is recorded passing; status remains `wired` (a single exercise is not `running`).
