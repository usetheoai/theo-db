---
milestone_id: M54
slug: vectorizer-declarative
date: 2026-07-07
status: in_progress
---
# Milestone M54 — Vectorizer próprio (auto-embedding declarativo)
## Objective (from ROADMAP.md)
Embedding gerado/atualizado em INSERT/UPDATE declarativamente (como pgai vectorizer/Supabase/AlloyDB). Hoje: zero triggers em sql/; app mantém todo embedding. Revisita ADR 0007 (async permitido p/ o vectorizer; função embed continua síncrona).
## DoD (5 itens)
1. Discover blueprint: pgai vectorizer + Supabase (trigger→fila→worker); decisão de worker (bgworker pgrx vs cron/externo) por ADR revisitando 0007.
2. theodb.create_vectorizer(table, source_col, embedding_col, model): trigger AFTER INSERT/UPDATE enfileira (jobs c/ estados tipados pending/processing/done/failed + tentativas); worker consome em batch via embed_batch; retry bounded reusa http.rs.
3. Chunking helper SQL (recursive character split, tamanho/overlap configuráveis).
4. Crash-safety da fila: processing c/ owner morto → pending (visibility timeout); e2e stub determinístico (INSERT→embedding; UPDATE→re-embed; falha endpoint→retry bounded+failed tipado).
5. Métrica runtime (wiring): contador jobs processados/falhados consultável.
Risco ALTO: bgworker pgrx (FFI novo, lifecycle/sinais/shutdown); fila crash-safe (visibilidade). Mitigação: discover primeiro, estados tipados, worker nunca segura transação do usuário.
## Outcome (parcial — em progresso, honesto)
**Feito e testado (commitado em develop):**
- **DoD 1 (discover):** blueprint `m54-vectorizer-declarative-blueprint.md` + ADR 0016 (bgworker in-process, revisita 0007). ✅
- **DoD 2 (fila + superfície):** `theodb.vectorizer_queue` crash-safe (estados tipados, fencing owner token, visibility-timeout, dead-letter); 4 `#[pg_extern]` de lógica (claim/mark_done/mark_failed/renew_lease); `theodb.create_vectorizer` + trigger `_vectorizer_enqueue` (enfileira em INSERT/UPDATE/DELETE, sem HTTP). ✅ (falta: o **worker** que consome a fila via embed_batch)
- **DoD 3 (chunking):** `theodb.chunk_text` (janela de caracteres com overlap, v1). ✅
- **DoD 4 (crash-safety):** provada via 6 pg_test (reclaim de owner morto, fencing anti-duplo-processamento, dead-letter, bounded retry). ✅ para a fila; **falta o e2e** (INSERT→embedding aparece) contra stub determinístico — exige o worker + container.
- 10 pg_test verdes no total (fila + trigger + chunking).

**Falta (não entregar sem isto — sem evidência 100% funcional não fecha):**
- **DoD 2 (worker):** bgworker pgrx (`theodb_embed_worker_main`) — loop claim→embed(PgTryBuilder catch)→upsert no target→mark. Território FFI novo (B1/B2 do blueprint). Registro via shared_preload_libraries.
- **DoD 4 (e2e):** teste e2e em container (preload) com stub de endpoint: INSERT→embedding aparece; UPDATE→re-embed; falha→retry bounded+failed.
- **DoD 5 (métrica):** contador de jobs processados/falhados consultável.
- Depois: `/review`, `/release` (v0.45.0), flip do checkbox.

Status: `in_progress` — o núcleo crash-safe (a parte de MAIOR risco de correção, provada por testes) está pronto; o worker FFI + e2e + métrica + release restam.
