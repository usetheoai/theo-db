# Implementation: M48 — Correctness & durabilidade do Index AM

> **Generated by `/implement m48-am-crash-safety`** — contrato de trabalho do halt-loop, derivado de
> `.claude/knowledge-base/plans/m48-am-crash-safety-plan.md`.

**Slug:** `m48-am-crash-safety`
**Source plan:** `.claude/knowledge-base/plans/m48-am-crash-safety-plan.md`
**Plan verdict:** `SHIPPABLE` (score `100.0`, 2026-07-05)
**Working branch:** `develop` (Unbreakable Rule 4)
**Started:** `2026-07-05`

## Pre-condition audit (verified at /implement start)

- [x] Plan verdict ≥ SHIPPABLE_WITH_CAVEATS (SHIPPABLE 100)
- [x] On `develop`
- [x] No uncommitted changes at start (porcelain vazio, HEAD f44818c)
- [x] Toolchain: Cargo.toml presente; builds via Docker (stage 1b `cargo pgrx install`); testes via pytest vs container
- [x] External deps up: Docker daemon OK; imagem `theodb:m48-verify` buildada; container de verificação disponível
- [x] TDD shape gate: `check_tdd_shape.py` all_pass=True

## Ambiente de execução (específico deste repo)

- Build: `docker build -t theodb:m48 .` (~2-4 min com cache; clippy/warnings visíveis no stage 1b)
- Testes: `PGHOST=localhost PGPORT=<porta> PGUSER=postgres PGPASSWORD=theodb python3 -m pytest benchmarks/tests/... -q`
- Container de teste: porta 55448 (`theodb-m48-verify`); crash tests gerenciam o próprio container
- Testes Rust puros: `cargo test` FALHA no host ($PGRX_HOME ausente) — testes `#[test]` puros de módulos
  sem pg_sys rodam via `cargo test --lib` DENTRO do builder stage OU são cobertos pelos pytest;
  registrar honestamente qual caminho foi usado por task.
- RED contra imagem ANTIGA (pré-fix), GREEN contra imagem rebuilt — o ciclo RED-GREEN atravessa um
  docker build por task.

## Task list (derived from plan; ordered by dependency)

| # | Plan ref | Status | Wiring (a) | Wiring (b) | Wiring (c) | Commit SHA |
|---|---|---|---|---|---|---|
| T1.1 | Phase 1 / T1.1 — #46 log_newpage_range + crash test UNLOGGED | pending | — | — | — | — |
| T2.1 | Phase 2 / T2.1 — meta v2 + fold.rs (shadow-write + pivot) | pending | — | — | — | — |
| T2.2 | Phase 2 / T2.2 — reclaim FSM + guard EC-2 | pending | — | — | — | — |
| T2.3 | Phase 2 / T2.3 — GUC crash-injection + 3 crash tests (#47 gate) | pending | — | — | — | — |
| T3.1 | Phase 3 / T3.1 — pending fold threshold no amvacuumcleanup | pending | — | — | — | — |
| T4.1 | Phase 4 / T4.1 — cancelabilidade (seam DIP) + vacuum_delay_point | pending | — | — | — | — |
| T5.1 | Phase 5 / T5.1 — amcostestimate real | pending | — | — | — | — |
| T6.1 | Phase 6 / T6.1 — benchmark artifact m48-am-maintenance | pending | — | — | — | — |

Status legend: `pending` / `red` / `green` / `refactor` / `wired` / `committed` / `blocked` / `done`

## Wiring triad — plano de evidência por task

- Pillar (a) caller: símbolos novos (`wal_log_init_fork`, `fold::fold`, GUCs, costestimate real) têm
  callers de produção nos callbacks do amroutine — verificar com check_wiring.py por símbolo.
- Pillar (b) integration: TODOS os testes são de integração (pytest vs container real) por natureza.
- Pillar (c) runtime metric: o plano declara pages_read (THEODB_SCAN_PROFILE) como métrica — observada
  não-zero nos testes de pending (T3.1) e registrada no artefato (T6.1).

## Global DoD (do plano — verificado no Step 5)

Ver `## Global Definition of Done` do plano. Chave: suíte inteira verde vs imagem rebuilt; clippy sem
warnings novos; CHANGELOG por fase; artefato de benchmark com mean±std ≥3 runs.

---

## Final Summary (halt-loop complete — 8/8 tasks committed)

| Task | Commit | Wiring (a/b/c) |
|---|---|---|
| T1.1 #46 INIT-fork WAL | `5286b44` | pass/pass/n/a |
| T2.1 crash-safe fold (meta-pivot, v3) | `aaf7684` | pass/pass/n/a |
| T2.2 bounded reclaim (ADR 0014) | `16a8a4f` | pass/pass/n/a |
| T2.3 crash-injection gate (superuser) | `5351260` | pass/pass/n/a |
| T3.1 pending fold + threshold + metric | `6d3d339` | pass/pass/pass |
| T4.1 cancellable build + vacuum_delay_point | `ea0a3f7` | pass/pass/n/a |
| T5.1 honest amcostestimate (am/cost.rs) | `d090b83` | pass/pass/n/a |
| T6.1 benchmark artifact | `72e54ad` | pass/pass/pass |

Plus `d4d3543` (page.rs comment condense), `f83b6e3` (ROADMAP M55 note).

**Issue closure:** #46 CLOSED (crash tests: UNLOGGED writable+scannable post-recovery). #47 silent corruption
ELIMINATED (meta-pivot + 3-point crash-injection: consistent-OR-fail-loud-REINDEX, never silently wrong;
residual reclaim window fail-loud → M55/ADR 0014).

**Benchmark (docs/benchmarks/m48-am-maintenance.md, 3 runs, N=50k):** fold above threshold 16 → pending 64→0,
p50 1.20→0.16 ms (~7×), WAL 12.28 MB±1.6 KB (M55 input); planner N=100 seqscan / N=50k index.

**Final validation (image theodb:m48-final): 76 tests passed, 0 failed** (maintenance 19 + crash 10 +
regression 47) + Rust unit am::cost 6 passed. run_validation: 5 PASS / 4 SKIP(npm) / 1 N/A / 1 FAIL
(acceptance_criteria file_size — unactionable gate/plan mismatch: CHANGELOG 678 can never be ≤500, hnsw_page.rs
& page.rs pre-existing; plan's real baseline+10% DoD met; split=committed M51 followup). Full rationale in
`m48-am-crash-safety-followups.md § Validation gate`. Surfaced to /review, not gamed (Rule 3).
