# Implementation: M169 — bugs de escala a 100M

> **Generated by `/implement m169-scale-bugs-100m`** — contrato de trabalho do halt-loop, derivado de
> `.claude/knowledge-base/plans/m169-scale-bugs-100m-plan.md`.

**Slug:** `m169-scale-bugs-100m`
**Source plan:** `.claude/knowledge-base/plans/m169-scale-bugs-100m-plan.md`
**Plan verdict:** _pendente_ — medido na box dedicada (ver ADR-4; o cap local é ambiental)
**Working branch:** `develop`
**Started:** `2026-07-29`

## Pre-condition audit (verificada no início)

- [x] On `develop`
- [x] Sem mudanças não-commitadas ao iniciar
- [x] Toolchain Rust presente (`rust-toolchain.toml` → 1.97.0)
- [x] `check_tdd_shape.py` → `all_pass=true` (T3.1/T3.2/T4.1 corrigidos nesta sessão; T4.1 não tinha seção TDD alguma)
- [x] Cabeçalhos de fase no literal que `check_phase_completeness.py:41` reconhece (eram `## Fase N`, o gate pularia em silêncio)
- [ ] Plan verdict ≥ SHIPPABLE_WITH_CAVEATS — **medido na box dedicada**, não aqui

## Onde cada gate roda (a divisão que o M168 ensinou)

| Tipo de gate | Onde | Por quê |
|---|---|---|
| Correção (`diverged=0`, byte-identidade, EXPLAIN, controle positivo) | qualquer box, a 1M | não envolve relógio |
| Medição (T1.2, T4.1, pico de pool, spill) | **`134.209.168.157`** (16 vCPU / 32 GB, dedicada) | a `165.227.121.20` é runner de CI e produziu rho=+1,00 de deriva |

## Task list (ordenada por dependência)

| # | Plan ref | Status | Wiring (a) | (b) | (c) | Commit SHA |
|---|---|---|---|---|---|---|
| T1.1 | Phase 1 / T1.1 | pending | — | — | — | — |
| T1.2 | Phase 1 / T1.2 | pending | — | — | — | — |
| T2.1 | Phase 2 / T2.1 | pending | — | — | — | — |
| T3.1 | Phase 3 / T3.1 | pending | — | — | — | — |
| T3.2 | Phase 3 / T3.2 | pending | — | — | — | — |
| T4.1 | Phase 4 / T4.1 | pending | — | — | — | — |

Status: `pending` / `red` / `green` / `refactor` / `wired` / `committed` / `blocked` / `done`
Wiring: `✓` passou · `✗` falhou · `defer` ADR-deferido · `—` não checado.

## Restrições que valem para TODAS as tasks

1. **`run_aggs_on_batch` (`df_executor.rs:619`) não muda.** Três chamadores; dois (`arrow_cache.rs:199` e `:265`)
   são o caminho heap-autoritativo do M101, sem nada de colunar. Acrescente um irmão streaming. Refatorá-lo
   contamina o cache Arrow do heap e **nenhum gate deste milestone perceberia**.
2. **EC-2 já está implementado** (`df_executor.rs:1132-1134`). Escreva o teste de regressão, não o código.
3. **Fail-open só por tipo**, via `find_root()` — nunca casando substring de mensagem (Regra 8; lição da rodada 10
   do M168, onde o catch-all fazia a consulta ignorar `statement_timeout`).
4. **Nenhuma alegação entra em documento antes de eu reproduzir a medição que a sustenta** — inclusive as que me
   favorecem. Nesta sessão essa regra já derrubou três diagnósticos meus (#219, #220, EC-2) e uma dispensa de gate
   por ADR que era desnecessária (ADR-4 reescrito).

## Per-task contract

O halt-loop preenche cada task progressivamente. Os detalhes por task (Files to edit, TDD, Acceptance criteria,
DoD) vivem no plano e **não são duplicados aqui** — este arquivo é o rastro de estado, não uma segunda cópia do
plano (DRY).
