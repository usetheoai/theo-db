# Dependencias externas das suites de teste do ferramental sob `.claude/`.
#
# POR QUE ESTE ARQUIVO EXISTE (B-090). O portao `tooling-tests` reprovou na PRIMEIRA corrida com
# `ModuleNotFoundError: No module named 'yaml'` em 20 testes de 6 slices. As suites passavam na
# maquina de desenvolvimento por ACIDENTE do ambiente — as dependencias nunca haviam sido
# declaradas em lugar nenhum, e ninguem podia saber quais eram sem tentar.
#
# E a mesma classe do resto desta sessao: algo que funcionava por um estado que ninguem registrou.
# O portao encontrou no primeiro uso, que e para o que ele serve.
#
# Levantadas por medicao, nao por memoria: varri os `import` de `skills/*/scripts`, `skills/*/tests`
# e `scripts/`, e separei o que resolve em `site-packages` do que resolve por `sys.path` local.
pytest>=7.4
pyyaml>=6.0
jsonschema>=4.0
hypothesis>=6.0
requests>=2.31

# O detector D2 (fabricacao de simbolo) usa tree-sitter. O modulo declara a dep como OPCIONAL e
# degrada para `auditor_unavailable_{tool}` sem ela — mas o CI deve exercitar o parser, entao ela
# entra aqui. Versoes fixadas nas que foram verificadas: `tree-sitter-languages 1.10.2` exige
# `tree-sitter < 0.22`, e soltar o piso quebraria a instalacao em vez de atualiza-la.
tree-sitter>=0.21,<0.22
tree-sitter-languages>=1.10

# O detector D1 (codigo morto) invoca o `vulture` como BINARIO no PATH, nao como biblioteca — por
# isso ele nao aparece na varredura de `import`. Descoberto pela segunda corrida do portao:
# `Vulture auditor unavailable: vulture not found in PATH`.
#
# Aqui a degradacao tambem e por projeto (`auditor_unavailable_{tool}` e soft cap no golden rule),
# mas os testes afirmam o caminho REAL sobre uma fixture limpa — e um detector que so foi exercitado
# degradado nao foi exercitado. `vulture` e o que o golden rule § 5 nomeia para Python.
vulture>=2.11
